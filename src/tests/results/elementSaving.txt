变量 {
    全局:
        0: A
        1: B
        2: C
    玩家:
        0: A
}
//Optimize for size enabled
规则 ("0 to null") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 添加至数组(全局.B, 空));
        设置全局变量(A, 数组(空, 1, 空));
        设置全局变量(A, 数组包含(全局.B, 空));
        设置全局变量(A, 比较(全局.B, ==, 空));
        设置全局变量(A, 比较(空, ==, 全局.B));
        设置全局变量(A, 比较(全局.B, !=, 空));
        设置全局变量(A, 比较(空, !=, 全局.B));
        设置全局变量(A, 自定义字符串("000"));
        设置全局变量(A, 字符串("{0}，{1}和{2}"));
        大字体信息(所有玩家(所有队伍), 空);
        设置全局变量(A, 空);
        在索引处设置全局变量(A, 全局.B, 空);
        设置玩家变量(全局.B, A, 空);
        在索引处设置玩家变量(全局.B, A, 全局.C, 空);
        小字体信息(所有玩家(所有队伍), 空);
    }
}

规则 ("0 to false") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 比较(全局.B, >, 空));
        设置全局变量(A, 比较(空, >, 全局.B));
        设置全局变量(A, 比较(全局.B, >=, 空));
        设置全局变量(A, 比较(空, >=, 全局.B));
        设置全局变量(A, 比较(全局.B, <, 空));
        设置全局变量(A, 比较(空, <, 全局.B));
        设置全局变量(A, 比较(全局.B, <=, 空));
        设置全局变量(A, 比较(空, <=, 全局.B));
        设置全局变量(A, 较大(假, 全局.B));
        设置全局变量(A, 较大(全局.B, 假));
        设置全局变量(A, 较小(假, 全局.B));
        设置全局变量(A, 较小(全局.B, 假));
        设置全局变量(A, 随机整数(假, 全局.B));
        设置全局变量(A, 随机整数(全局.B, 假));
        设置全局变量(A, 减(假, 全局.B));
        设置全局变量(A, 加(左, 右));
        追踪全局变量频率(A, 假, 假, 全部禁用);
        持续追踪全局变量(A, 假, 假, 全部禁用);
        追踪玩家变量频率(全局.B, A, 假, 假, 全部禁用);
        持续追踪玩家变量(全局.B, A, 假, 假, 全部禁用);
        生成机器人(英雄(安娜), 队伍2, 假, 全局.B, 全局.C);
        创建HUD文本(所有玩家(所有队伍), 自定义字符串("123"), 空, 空, 左边, 假, 颜色(白色), 空, 空, 可见, 始终可见);
        For 全局变量(A, 0, 假, 假);
        End;
        For 玩家变量(全局.B, A, 0, 假, 假);
        End;
        在索引处设置全局变量(A, 假, 全局.B);
        在索引处设置玩家变量(全局.B, A, 假, 全局.C);
        等待(假, 无视条件);
        等待(假, 无视条件);
        等待(假, 无视条件);
    }
}

规则 ("1 to true") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 加(全局.B, 真));
        设置全局变量(A, 加(真, 全局.B));
        设置全局变量(A, 比较(全局.B, >, 1));
        设置全局变量(A, 比较(1, >, 全局.B));
        设置全局变量(A, 比较(全局.B, >=, 1));
        设置全局变量(A, 比较(1, >=, 全局.B));
        设置全局变量(A, 比较(全局.B, <, 1));
        设置全局变量(A, 比较(1, <, 全局.B));
        设置全局变量(A, 比较(全局.B, <=, 1));
        设置全局变量(A, 比较(1, <=, 全局.B));
        设置全局变量(A, 除(真, 全局.B));
        设置全局变量(A, 较大(真, 全局.B));
        设置全局变量(A, 较大(全局.B, 真));
        设置全局变量(A, 较小(真, 全局.B));
        设置全局变量(A, 较小(全局.B, 真));
        设置全局变量(A, 随机整数(真, 全局.B));
        设置全局变量(A, 随机整数(全局.B, 真));
        设置全局变量(A, 减(真, 全局.B));
        设置全局变量(A, 减(全局.B, 真));
        设置全局变量(A, 数组中的值(全局.B, 真));
        设置全局变量(A, 矢量(真, 真, 真));
        追踪全局变量频率(A, 真, 真, 全部禁用);
        持续追踪全局变量(A, 真, 真, 全部禁用);
        追踪玩家变量频率(全局.B, A, 真, 真, 全部禁用);
        持续追踪玩家变量(全局.B, A, 真, 真, 全部禁用);
        生成机器人(英雄(安娜), 队伍2, 真, 全局.B, 全局.C);
        创建HUD文本(所有玩家(所有队伍), 自定义字符串("123"), 空, 空, 左边, 真, 颜色(白色), 空, 空, 可见, 始终可见);
        For 全局变量(A, 1, 真, 真);
        End;
        For 玩家变量(全局.B, A, 1, 真, 真);
        End;
        在索引处设置全局变量(A, 真, 全局.B);
        在索引处设置玩家变量(全局.B, A, 真, 全局.C);
        等待(真, 无视条件);
    }
}

规则 ("null vector to null") {
    事件 {
        持续 - 全局;
    }
    动作 {
        生成机器人(英雄(安娜), 队伍2, 全局.A, 空, 空);
    }
}

规则 ("other optimizations") {
    事件 {
        持续 - 全局;
    }
    动作 {
        "left + right"
        设置全局变量(A, 加(左, 右));
        "left"
        设置全局变量(A, 左);
        "left"
        设置全局变量(A, 左);
        "right"
        设置全局变量(A, 右);
        "right"
        设置全局变量(A, 右);
        "up"
        设置全局变量(A, 上);
        "up"
        设置全局变量(A, 上);
        "down"
        设置全局变量(A, 下);
        "down"
        设置全局变量(A, 下);
        "forward"
        设置全局变量(A, 前);
        "forward"
        设置全局变量(A, 前);
        "backward"
        设置全局变量(A, 后);
        "backward"
        设置全局变量(A, 后);
        "B * left"
        设置全局变量(A, 乘(全局.B, 左));
        "B * up"
        设置全局变量(A, 乘(全局.B, 上));
        "B * forward"
        设置全局变量(A, 乘(全局.B, 前));
    }
}

