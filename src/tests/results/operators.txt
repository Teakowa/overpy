变量 {
    全局:
        0: A
        1: B
        2: C
        3: D
    玩家:
        0: A
}
//Optimize for size enabled
规则 ("strict optimizations") {
    事件 {
        持续 - 全局;
    }
    动作 {
        //Strict optimizations enabled
        "Unless specified otherwise, none of these should be optimized"
        设置全局变量(A, 比较(自定义字符串("am"), ==, 自定义字符串("**")));
        设置全局变量(A, 加(全局.B, 0));
        设置全局变量(A, 加(全局.B, 加(左, 右)));
        设置全局变量(A, 乘(全局.B, 0));
        设置全局变量(A, 乘(全局.B, 0));
        "0"
        设置全局变量(A, 空);
        设置全局变量(A, 乘(全局.B, 1));
        设置全局变量(A, 除(全局.B, 1));
        设置全局变量(A, 除(0, 全局.B));
        设置全局变量(A, 与(全局.B, 假));
        设置全局变量(A, 与(全局.B, 真));
        设置全局变量(A, 与(全局.B, 非(全局.B)));
        设置全局变量(A, 或(全局.B, 假));
        设置全局变量(A, 或(全局.B, 真));
        设置全局变量(A, 或(全局.B, 非(全局.B)));
    }
}

规则 ("comparison") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 比较(全局.B, ==, 全局.C));
        "false"
        设置全局变量(A, 假);
        "true"
        设置全局变量(A, 真);
        "not B"
        设置全局变量(A, 比较(全局.B, ==, 空));
        "isGameInProgress()"
        设置全局变量(A, 游戏正在进行中);
        "false"
        设置全局变量(A, 假);
        "should not be optimized"
        设置全局变量(A, 比较(英雄(安娜), ==, 自定义字符串("azer{0}", 全局.A)));
        设置全局变量(A, 比较(全局.B, !=, 全局.C));
        "true"
        设置全局变量(A, 真);
        "false"
        设置全局变量(A, 假);
        设置全局变量(A, 比较(全局.B, !=, 空));
        "not B"
        设置全局变量(A, 非(比较(全局.B, !=, 空)));
        "not isGameInProgress()"
        设置全局变量(A, 非(游戏正在进行中));
        "true"
        设置全局变量(A, 真);
        "should not be optimized"
        设置全局变量(A, 比较(英雄(安娜), !=, 自定义字符串("azer{0}", 全局.A)));
        设置全局变量(A, 比较(全局.B, >=, 全局.C));
        "true"
        设置全局变量(A, 真);
        "true"
        设置全局变量(A, 真);
        设置全局变量(A, 比较(全局.B, <=, 全局.C));
        "false"
        设置全局变量(A, 假);
        "true"
        设置全局变量(A, 真);
        设置全局变量(A, 比较(全局.B, >, 全局.C));
        "true"
        设置全局变量(A, 真);
        "false"
        设置全局变量(A, 假);
        设置全局变量(A, 比较(全局.B, <, 全局.C));
        "false"
        设置全局变量(A, 假);
        "false"
        设置全局变量(A, 假);
    }
}

规则 ("add") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 加(全局.B, 全局.C));
        "B"
        设置全局变量(A, 全局.B);
        "B*2"
        设置全局变量(A, 乘(2, 全局.B));
        "3"
        设置全局变量(A, 3);
        "B"
        设置全局变量(A, 全局.B);
        "B"
        设置全局变量(A, 全局.B);
        设置全局变量(A, 加(矢量(全局.B, 全局.C, 全局.D), 矢量(真, 2, 3)));
        "vect(5,7,9)"
        设置全局变量(A, 矢量(5, 7, 9));
        "left + right"
        设置全局变量(A, 加(左, 右));
        "up"
        设置全局变量(A, 上);
        "vect(1, -1, -1)"
        设置全局变量(B, 矢量(真, -1, -1));
        "vect(-1, 1, 1)"
        设置全局变量(C, 矢量(-1, 真, 真));
    }
}

规则 ("subtract") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 减(全局.B, 全局.C));
        设置全局变量(A, 减(减(全局.B, 全局.C), 全局.D));
        "B"
        设置全局变量(A, 全局.B);
        "0"
        设置全局变量(A, 空);
        "-1"
        设置全局变量(A, -1);
        "B"
        设置全局变量(A, 全局.B);
        设置全局变量(A, 减(矢量(全局.B, 全局.C, 全局.D), 矢量(真, 2, 3)));
        "vect(-3,-3,-3)"
        设置全局变量(A, 矢量(-3, -3, -3));
    }
}

规则 ("negate") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 乘(-1, 全局.B));
        设置全局变量(A, 余数(乘(-1, 全局.B), 全局.C));
        设置全局变量(A, 乘(-1, 矢量(真, 全局.B, -2)));
        "vect(-1,-2,3)"
        设置全局变量(A, 矢量(-1, -2, 3));
        设置全局变量(A, -1);
        设置全局变量(A, 乘(-1, 全局.B));
        "B"
        设置全局变量(A, 全局.B);
        "1"
        设置全局变量(A, 1);
        "B"
        设置全局变量(A, 全局.B);
        "-1"
        设置全局变量(A, -1);
        "-B"
        设置全局变量(A, 乘(-1, 全局.B));
        "0.01"
        设置全局变量(A, 0.01);
        设置全局变量(A, 加(全局.B, 乘(-1, 全局.C)));
        设置全局变量(A, 非(乘(-1, 全局.C)));
        设置全局变量(A, 乘(全局.B, 乘(-1, 全局.C)));
    }
}

规则 ("multiply") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 乘(全局.B, 全局.C));
        "B"
        设置全局变量(A, 全局.B);
        "B**2"
        设置全局变量(A, 乘(全局.B, 全局.B));
        "0"
        设置全局变量(A, 空);
        "6"
        设置全局变量(A, 6);
        "vect(6,8,10)"
        设置全局变量(A, 矢量(6, 8, 10));
        "vect(6,8,10)"
        设置全局变量(A, 矢量(6, 8, 10));
        "vect(4,10,18)"
        设置全局变量(A, 矢量(4, 10, 18));
    }
}

规则 ("divide") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 除(全局.B, 全局.C));
        "B"
        设置全局变量(A, 全局.B);
        "0"
        设置全局变量(A, 空);
        "0"
        设置全局变量(A, 空);
        "0.5"
        设置全局变量(A, 0.5);
        设置全局变量(A, 除(-3, 全局.B));
        "vect(3,1.5,1)"
        设置全局变量(A, 矢量(3, 1.5, 真));
        "vect(0.5,1,1.5)"
        设置全局变量(A, 矢量(0.5, 真, 1.5));
    }
}

规则 ("modulo") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 余数(全局.B, 全局.C));
        "0"
        设置全局变量(A, 空);
        "0"
        设置全局变量(A, 空);
        "0"
        设置全局变量(A, 空);
        "1"
        设置全局变量(A, 1);
        "-1"
        设置全局变量(A, -1);
        "-1"
        设置全局变量(A, -1);
        "1"
        设置全局变量(A, 1);
    }
}

规则 ("power") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 乘方(全局.B, 全局.C));
        设置全局变量(A, 乘方(全局.B, 乘方(全局.C, 全局.D)));
        "B"
        设置全局变量(A, 全局.B);
        "0"
        设置全局变量(A, 空);
        "1"
        设置全局变量(A, 1);
        "0"
        设置全局变量(A, 乘方(-1, 全局.B));
        "8"
        设置全局变量(A, 8);
        "0"
        设置全局变量(A, 空);
    }
}

规则 ("assignment") {
    事件 {
        持续 - 每名玩家;
        双方;
        全部;
    }
    动作 {
        设置全局变量(A, 全局.B);
        在索引处设置全局变量(A, 假, 全局.B);
        设置玩家变量(事件玩家, A, 全局.B);
        在索引处设置玩家变量(事件玩家, A, 假, 全局.B);
        修改全局变量(A, 加, 全局.B);
        修改玩家变量(事件玩家, A, 加, 全局.B);
        修改全局变量(A, 减, 全局.B);
        修改全局变量(A, 乘, 全局.B);
        修改全局变量(A, 除, 全局.B);
        修改全局变量(A, 余数, 全局.B);
        修改全局变量(A, 乘方, 全局.B);
        修改全局变量(A, 最大, 全局.B);
        修改全局变量(A, 最小, 全局.B);
        修改全局变量(A, 加, 真);
        修改全局变量(A, 减, 真);
    }
}

规则 ("and") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 与(全局.A, 全局.B));
        "B"
        设置全局变量(A, 全局.B);
        "false"
        设置全局变量(A, 假);
        "B"
        设置全局变量(A, 全局.B);
        "false"
        设置全局变量(A, 假);
        "not (B or C)"
        设置全局变量(A, 非(或(全局.B, 全局.C)));
    }
}

规则 ("or") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 或(全局.A, 全局.B));
        "B"
        设置全局变量(A, 全局.B);
        "true"
        设置全局变量(A, 真);
        "B"
        设置全局变量(A, 全局.B);
        "true"
        设置全局变量(A, 真);
        "not (B and C)"
        设置全局变量(A, 非(与(全局.B, 全局.C)));
    }
}

规则 ("not") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 非(全局.B));
        "not not B"
        设置全局变量(A, 非(非(全局.B)));
        "B == C"
        设置全局变量(A, 比较(全局.B, ==, 全局.C));
        "false"
        设置全局变量(A, 假);
        "true"
        设置全局变量(A, 真);
        设置全局变量(A, 乘(3, 非(全局.B)));
    }
}

规则 ("in") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 数组包含(全局.B, 全局.A));
        "true"
        设置全局变量(A, 真);
        设置全局变量(A, 比较(3, ==, 全局.A));
        设置全局变量(A, 非(数组包含(全局.C, 全局.B)));
    }
}

规则 ("ternaries") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, If-Then-Else(全局.C, 全局.B, 全局.D));
        "B"
        设置全局变量(A, 全局.B);
        "D"
        设置全局变量(A, 全局.D);
        "B"
        设置全局变量(A, 全局.B);
        "if-then-else(C, D, B)"
        设置全局变量(A, If-Then-Else(全局.C, 全局.D, 全局.B));
        "D"
        设置全局变量(A, 全局.D);
    }
}

规则 ("handling random values") {
    事件 {
        持续 - 全局;
    }
    动作 {
        修改玩家变量(数组随机取值(所有玩家(所有队伍)), A, 加, 真);
        设置玩家变量(数组随机取值(所有玩家(所有队伍)), A, 加((数组随机取值(所有玩家(所有队伍))).A, 真));
        修改玩家变量(数组随机取值(所有玩家(所有队伍)), A, 最大, 全局.B);
    }
}

