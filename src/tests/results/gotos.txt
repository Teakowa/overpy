变量 {
    全局:
        0: A
}
//Optimize for size enabled
规则 ("gotos") {
    事件 {
        持续 - 全局;
    }
    动作 {
        "skip 3"
        跳过(真);
        If(假);
            //lbl_0:
            设置全局变量(A, 2);
        End;
        If(假);
            //lbl_1:
        End;
        跳过(全局.A);
        "skip 3"
        跳过(3);
        "pass"
        禁用 中止;
        //lbl_3:
        If(假);
            设置全局变量(A, 1);
            //lbl_2:
            设置全局变量(A, 2);
    }
}

