设置
{
	主程序
	{
		模式名称: "Some awesome game mode"
		描述: "OverPy starter pack"
	}
	模式
	{
		突击模式
		{
			启用地图
			{
				地图工坊岛屿
			}
		}
		综合
		{
			英雄限制: 关闭
			复生时间: 30%
		}
	}
}
变量 {
    全局:
        0: counter
        127: __holygrail__
}
规则 ("Disable inspector") {
    事件 {
        持续 - 全局;
    }
    动作 {
        禁用查看器录制;
    }
}

规则 ("<fg00FFFFFF>OverPy <­tx> / <­fg> setup code</fg>") {
    事件 {
        持续 - 全局;
    }
    动作 {
        "By Zezombye"
        生成机器人(全部英雄, If-Then-Else(栏位数量(队伍1), 队伍1, If-Then-Else(栏位数量(队伍2), 队伍2, 真)), 假, 空, 空);
        "More info: https://workshop.codes/wiki/articles/tx-reference-sheet"
        开始为机器人强制设置名称(最后创建的实体, 自定义字符串("______________________________________________________________________________________________________________________________〼"));
        设置全局变量(__holygrail__, 字符串分割(首个(最后创建的实体), 空数组));
        开始为机器人强制设置名称(最后创建的实体, 自定义字符串("______________________________________________________________________________________________________________________________ࡀ"));
        设置全局变量(__holygrail__, 截取字符串(字符串替换(自定义字符串("______________________________________________________________________________________________________________________________〼"), 全局.__holygrail__, 首个(最后创建的实体)), 126, 真));
        移除所有机器人;
    }
}

规则 ("Teleport player on pressing interact") {
    事件 {
        持续 - 每名玩家;
        双方;
        全部;
    }
    条件 {
        按钮被按下(事件玩家, 按钮(互动)) == 真;
    }
    动作 {
        修改全局变量(counter, 加, 1);
        传送(事件玩家, 加(眼睛位置(事件玩家), 乘(面朝方向(事件玩家), 5)));
        "Hold the player in place, to reset falling velocity"
        开始强制设置玩家位置(事件玩家, 所选位置(事件玩家), 假);
        等待(0.016, 无视条件);
        停止强制设置玩家位置(事件玩家);
    }
}

规则 ("Display position") {
    事件 {
        持续 - 每名玩家;
        双方;
        全部;
    }
    动作 {
        创建HUD文本(所有玩家(所有队伍), 自定义字符串("{0}txc00000000020af8>Position of {1}: {0}fg00FFFFFF>{2}{0}/fg>", 全局.__holygrail__, 事件玩家, 所选位置(事件玩家)), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 空, 自定义字符串("{0}FGD0FF00FF>ƈουṇṭẹṛ {0}FG99F3FFFF>= {0}FGFFFF00FF>{1}                                                                      {2}", 全局.__holygrail__, 映射的数组(映射的数组(数组(映射的数组(全局.counter, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("{0}, {1}, {2}", 数组中的值(当前数组元素, 2), 数组中的值(当前数组元素, 3), 自定义字符串("{0}, {1}, {2}", 数组中的值(当前数组元素, 4), 数组中的值(当前数组元素, 5), 自定义字符串("{0}, {1}, …", 数组中的值(当前数组元素, 6), 数组中的值(当前数组元素, 7)))), 截取字符串(自定义字符串("0, 0, 0, 0, 0, 0, …"), 加(-2, 数组中的值(当前数组元素, 1)), 减(22, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 18), 自定义字符串("+{0}", 减(除(数组中的值(当前数组元素, 1), 3), 6)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                    ")), 空, 左边, -9999, 空, 颜色(白色), 空, 可见，排序规则，字符串和颜色, 默认可见度);
    }
}

