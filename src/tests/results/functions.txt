变量 {
    全局:
        0: A
        1: B
        2: C
        3: D
        4: E
        23: X
        24: Y
        25: Z
    玩家:
        1: B
}
//Optimize for size enabled
规则 ("function") {
    事件 {
        持续 - 每名玩家;
        双方;
        全部;
    }
    动作 {
        设置全局变量(A, 射线命中玩家(全局.B, 全局.C, 全局.D, 全局.E, 真));
        设置全局变量(A, 射线命中位置(全局.B, 全局.C, 全局.D, 全局.E, 假));
        设置全局变量(A, 射线命中法线(全局.B, 全局.C, 全局.D, 全局.E, 真));
        设置全局变量(A, 除(除(乘(乘(-3, 999999999999), 999999999999), 999999999999), 10));
        设置全局变量(A, 截取字符串(全局.B, 2, 4));
        持续追踪玩家变量(事件玩家, B, 9999, 3, 终点及持续时间);
        持续追踪玩家变量(事件玩家, B, 9999, 3, 终点及持续时间);
        追踪玩家变量频率(事件玩家, B, 9999, 3, 速率及最终值);
        持续追踪全局变量(B, 9999, 真, 终点及持续时间);
        追踪全局变量频率(B, 99999, 真, 速率及最终值);
        创建HUD文本(所有玩家(所有队伍), 自定义字符串("test"), 空, 空, 左边, 真, 颜色(红色), 空, 空, 可见和字符串, 始终可见);
        创建HUD文本(所有玩家(所有队伍), 空, 空, 自定义字符串("test"), 左边, 真, 空, 空, 颜色(红色), 可见和字符串, 始终可见);
        创建HUD文本(所有玩家(所有队伍), 空, 自定义字符串("test"), 空, 左边, 真, 空, 颜色(红色), 空, 可见和字符串, 始终可见);
        创建HUD文本(空, 自定义字符串("azer"), 自定义字符串("dqsf"), 自定义字符串("fdsq"), 左边, -1, 颜色(红色), 颜色(白色), 颜色(黄色), 可见性，排序规则和字符串, 始终不可见);
        等待(假, 无视条件);
        设置全局变量(A, 距离最近的玩家(全局.B, 全局.C));
        设置全局变量(A, 首个(已排序的数组(已过滤的数组(所有存活玩家(全局.C), 已重生(当前数组元素)), 相距距离(当前数组元素, 全局.B))));
        设置全局变量(A, 首个(已排序的数组(已过滤的数组(所有存活玩家(全局.C), 已重生(当前数组元素)), 相距距离(当前数组元素, 全局.B))));
        设置全局变量(A, 距离最远的玩家(全局.B, 全局.C));
        设置全局变量(A, 首个(已排序的数组(已过滤的数组(所有存活玩家(全局.C), 已重生(当前数组元素)), 乘(-1, 相距距离(当前数组元素, 全局.B)))));
        设置全局变量(A, 首个(已排序的数组(已过滤的数组(所有存活玩家(全局.C), 已重生(当前数组元素)), 乘(-1, 相距距离(当前数组元素, 全局.B)))));
        设置全局变量(A, 距离准星最近的玩家(全局.A, 全局.B));
        设置全局变量(A, 首个(已排序的数组(已过滤的数组(所有存活玩家(全局.B), 与(已重生(当前数组元素), 比较(当前数组元素, !=, 全局.A))), 矢量间夹角(面朝方向(全局.A), 减(当前数组元素, 眼睛位置(全局.A))))));
        设置全局变量(A, 首个(已排序的数组(已过滤的数组(所有存活玩家(全局.B), 与(已重生(当前数组元素), 比较(当前数组元素, !=, 全局.A))), 矢量间夹角(面朝方向(全局.A), 减(当前数组元素, 眼睛位置(全局.A))))));
        设置全局变量(A, 比较(矢量间夹角(全局.B, 数组随机取值(全局.C)), <=, 以角度为单位的反正弦值(除(数组随机取值(全局.E), 相距距离(全局.B, 全局.D)))));
        "Color.YELLOW"
        设置全局变量(A, 颜色(黄色));
    }
}

规则 ("beams") {
    事件 {
        持续 - 全局;
    }
    动作 {
        创建光束效果(全局.A, 抓钩光束, 全局.B, 全局.C, 空, 可见，位置和半径);
        创建光束效果(全局.A, 抓钩光束, 矢量(全局.A, 全局.B, 全局.C), 矢量(全局.A, 全局.D, 全局.C), 空, 可见，位置和半径);
        创建光束效果(全局.A, 抓钩光束, 矢量(全局.Z, 全局.B, 全局.C), 矢量(全局.A, 全局.D, 全局.C), 空, 可见，位置和半径);
        创建光束效果(全局.A, 有益光束, 矢量(全局.A, 全局.B, 全局.C), 矢量(全局.A, 全局.D, 全局.C), 颜色(白色), 可见，位置和半径);
        创建光束效果(全局.A, 有益光束, 矢量(全局.A, 全局.B, 全局.C), 矢量(全局.A, 全局.D, 全局.C), 颜色(白色), 可见，位置和半径);
        创建光束效果(全局.A, 有害光束, 矢量(全局.A, 全局.B, 全局.C), 矢量(全局.A, 全局.D, 全局.C), 颜色(白色), 可见，位置和半径);
        创建光束效果(全局.A, 有害光束, 矢量(全局.A, 全局.B, 全局.C), 矢量(全局.A, 全局.D, 全局.Z), 颜色(白色), 可见，位置和半径);
    }
}

规则 ("vectors") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 矢量(全局.B, 全局.C, 全局.D));
        设置全局变量(A, X方向分量(全局.B));
        "B"
        设置全局变量(A, 全局.B);
        "C"
        设置全局变量(A, 全局.C);
        "D"
        设置全局变量(A, 全局.D);
        "Vector.UP"
        设置全局变量(A, 上);
    }
}

规则 ("optimizations with literals") {
    事件 {
        持续 - 全局;
    }
    动作 {
        "4"
        修改全局变量(A, 添加至数组, 4);
        "3"
        修改全局变量(A, 添加至数组, 3);
        "4"
        修改全局变量(A, 添加至数组, 4);
        "3"
        修改全局变量(A, 添加至数组, 3);
        "5"
        修改全局变量(A, 添加至数组, 5);
        "1.012"
        修改全局变量(A, 添加至数组, 1.012195761452095);
        "3.1415"
        修改全局变量(A, 添加至数组, 3.141592653589793);
        "113.633"
        修改全局变量(A, 添加至数组, 0.034597891672104);
        "-0.302"
        修改全局变量(A, 添加至数组, -0.304692654015397);
        "36.865"
        修改全局变量(A, 添加至数组, 0.011231213088677);
        "1.584"
        修改全局变量(A, 添加至数组, 1.583839065425504);
        "140.057"
        修改全局变量(A, 添加至数组, 0.042663877670413);
        "0.996"
        修改全局变量(A, 添加至数组, 0.996090498505521);
        "-0.934"
        修改全局变量(A, 添加至数组, -0.933580426497201);
        "vect(3951, -3165, -6406)"
        修改全局变量(A, 添加至数组, 矢量(3951, -3165, -6406));
        "vect(-0.182, 0.273, 0.945)"
        修改全局变量(A, 添加至数组, 矢量(-0.182127177225397, 0.273190765838096, 0.944561536786621));
        "1014.740"
        修改全局变量(A, 添加至数组, 1014.7403608805555);
        "43498"
        修改全局变量(A, 添加至数组, 43498);
        "Team 2"
        修改全局变量(A, 添加至数组, 队伍2);
        "Team 1"
        修改全局变量(A, 添加至数组, 队伍1);
        "All Teams"
        修改全局变量(A, 添加至数组, 所有队伍);
        "0"
        修改全局变量(A, 添加至数组, 空);
        "3"
        修改全局变量(A, 添加至数组, 3);
        "4"
        修改全局变量(A, 添加至数组, 4);
        "13.287"
        修改全局变量(A, 添加至数组, 13.28771237954945);
        "9.210"
        修改全局变量(A, 添加至数组, 9.210340371976184);
        "32"
        修改全局变量(A, 添加至数组, 32);
        "-31"
        修改全局变量(A, 添加至数组, -31);
        "vect(-0.017, 0.433, 0.901)"
        修改全局变量(A, 添加至数组, 矢量(-0.017380968387966, 0.433187212130861, 0.901136361037656));
        "0.704"
        修改全局变量(A, 添加至数组, 0.703595051234721);
        "-0.218"
        修改全局变量(A, 添加至数组, -0.218143241396542);
        "0"
        修改全局变量(A, 添加至数组, 空);
        "9.656"
        修改全局变量(A, 添加至数组, 9.655568341635826);
        "1.364"
        修改全局变量(A, 添加至数组, 1.363873635438722);
        "-36.648"
        修改全局变量(A, 添加至数组, -36.64847484990379);
        "vect(342, -88, 68)"
        修改全局变量(A, 添加至数组, 矢量(342, -88, 68));
    }
}

规则 ("optimizations for function combinations") {
    事件 {
        持续 - 全局;
    }
    动作 {
        "directionTowards(x, y)"
        设置全局变量(A, 方向(全局.X, 全局.Y));
        "is dead(B)"
        设置全局变量(A, 死亡(全局.B));
        "is alive(B)"
        设置全局变量(A, 存活(全局.B));
        "getPlayersOnHero(C, B)"
        设置全局变量(A, 选择英雄的玩家(全局.C, 全局.B));
        "getPlayersOnObjective(B)"
        设置全局变量(A, 所有目标点内玩家(全局.B));
        "getPlayersNotOnObjective(B)"
        设置全局变量(A, 所有目标点外玩家(全局.B));
        "getLivingPlayers(B)"
        设置全局变量(A, 所有存活玩家(全局.B));
        "getDeadPlayers(B)"
        设置全局变量(A, 所有死亡玩家(全局.B));
        "getNumberOfPlayers(B)"
        设置全局变量(A, 玩家数量(全局.B));
        "getNumberOfPlayersOnObjective(B)"
        设置全局变量(A, 目标点上玩家数量(全局.B));
        "getNumberOfHeroes(B, C)"
        设置全局变量(A, 英雄数量(全局.B, 全局.C));
        "getNumberOfLivingPlayers(B)"
        设置全局变量(A, 存活玩家数量(全局.B));
        "getNumberOfDeadPlayers(B)"
        设置全局变量(A, 死亡玩家数量(全局.B));
    }
}

规则 ("default arguments") {
    事件 {
        持续 - 全局;
    }
    动作 {
        创建HUD文本(所有玩家(所有队伍), 1234, 空, 空, 左边, 假, 颜色(白色), 空, 空, 可见，排序规则，字符串和颜色, 默认可见度);
        创建HUD文本(全局.A, 1234, 空, 空, 左边, 假, 颜色(白色), 空, 空, 可见，排序规则，字符串和颜色, 默认可见度);
        创建HUD文本(全局.A, 1234, 空, 空, 左边, 假, 颜色(白色), 空, 空, 可见，排序规则，字符串和颜色, 默认可见度);
        创建HUD文本(全局.A, 1234, 空, 空, 左边, 假, 颜色(白色), 空, 空, 可见，排序规则，字符串和颜色, 默认可见度);
        等待(假, 无视条件);
        开始朝向(全局.C, 矢量(真, 2, 3), 999999999999, 至地图, 方向及角速率);
        开始朝向(全局.C, 矢量(真, 2, 3), 999999999999, 至玩家, 方向及角速率);
        设置全局变量(D, 弹药(全局.D, 假));
        设置全局变量(E, 数组分割(全局.E, 5, 999999999999));
        设置全局变量(E, 数组分割(全局.E, 5, 7));
    }
}

