变量 {
    全局:
        0: A
        1: B
        2: C
        3: D
        4: E
        23: X
        24: Y
        25: Z
    玩家:
        0: A
}
规则 ("array literals") {
    事件 {
        持续 - 全局;
    }
    动作 {
        "empty array"
        设置全局变量(A, 空数组);
        设置全局变量(A, 数组(1, 2, 3));
        设置全局变量(A, 数组中的值(全局.A, 全局.B));
        "first of(B)"
        设置全局变量(A, 首个(全局.B));
        "B"
        设置全局变量(A, 全局.B);
        "C"
        设置全局变量(A, 全局.C);
        "0"
        设置全局变量(A, 空);
        "C"
        设置全局变量(A, 全局.C);
        "D"
        设置全局变量(A, 全局.D);
        "D"
        设置全局变量(A, 全局.D);
        "0"
        设置全局变量(A, 空);
        设置全局变量(A, 数组(数组(1, 2), 数组(3, 4)));
        在索引处设置全局变量(A, 0, 首个(全局.B));
    }
}

规则 ("last") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 最后(全局.B));
        "D"
        设置全局变量(A, 全局.D);
    }
}

规则 ("filtered array") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 已过滤的数组(所有玩家(所有队伍), 比较((当前数组元素).A, ==, 2)));
        设置全局变量(A, 已过滤的数组(所有玩家(所有队伍), 比较(当前数组元素, !=, 比较(全局.B, ==, 当前数组元素))));
        "getAllPlayers.remove(B)"
        设置全局变量(A, 从数组中移除(所有玩家(所有队伍), 全局.B));
        "getAllPlayers() if A == B else []"
        设置全局变量(A, If-Then-Else(比较(全局.A, ==, 全局.B), 所有玩家(所有队伍), 空数组));
        "getAllPlayers()"
        设置全局变量(A, 所有玩家(所有队伍));
        设置全局变量(A, 已过滤的数组(所有玩家(所有队伍), 比较(当前数组元素, !=, 当前数组索引)));
        设置全局变量(A, 映射的数组(已过滤的数组(所有玩家(所有队伍), 比较((当前数组元素).A, ==, 2)), 生命值(当前数组元素)));
        "mapped array()"
        设置全局变量(A, 映射的数组(所有玩家(所有队伍), 生命值(当前数组元素)));
    }
}

规则 ("mapped array") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 映射的数组(所有玩家(所有队伍), 生命值(当前数组元素)));
        "getAllPlayers()"
        设置全局变量(A, 所有玩家(所有队伍));
    }
}

规则 ("index") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 数组值的索引(全局.B, 3));
        "[A,B].index(B)"
        设置全局变量(A, 数组值的索引(数组(全局.A, 全局.B), 全局.B));
        "-1"
        设置全局变量(A, -1);
        "2"
        设置全局变量(A, 2);
        "should not be optimized"
        设置全局变量(A, 数组值的索引(数组(1, 2, 全局.B, 3), 3));
        "-1"
        设置全局变量(A, -1);
        "should not be optimized"
        设置全局变量(A, 数组值的索引(数组(1, 2, 3, 全局.B), 123));
    }
}

规则 ("slice") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 数组分割(全局.B, 1, 3));
        "[4]"
        设置全局变量(A, 数组(4));
        "[4,5]"
        设置全局变量(A, 数组(4, 5));
        "[]"
        设置全局变量(A, 空数组);
        "[]"
        设置全局变量(A, 空数组);
        "[1]"
        设置全局变量(A, 数组(1));
        "[1,2,3]"
        设置全局变量(A, 数组(1, 2, 3));
    }
}

规则 ("in") {
    事件 {
        持续 - 全局;
    }
    动作 {
        "true"
        设置全局变量(A, 真);
        "true"
        设置全局变量(A, 真);
        "false"
        设置全局变量(A, 假);
        "false"
        设置全局变量(A, 假);
        "123 == B"
        设置全局变量(A, 比较(123, ==, 全局.B));
    }
}

规则 ("exclude") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 从数组中移除(全局.B, 3));
        "should not be optimized"
        设置全局变量(A, 从数组中移除(数组(全局.B, 全局.C, 全局.D), 全局.C));
        "[1,3]"
        设置全局变量(A, 数组(1, 3));
        "[1,D].exclude(2)"
        设置全局变量(A, 从数组中移除(数组(1, 全局.D), 2));
        "[2]"
        设置全局变量(A, 数组(2));
        "[2,3].exclude([D])"
        设置全局变量(A, 从数组中移除(数组(2, 3), 数组(全局.D)));
    }
}

规则 ("concat") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 添加至数组(全局.B, 2));
        "[B,C,123]"
        设置全局变量(A, 数组(全局.B, 全局.C, 123));
        "should not be optimized"
        设置全局变量(A, 添加至数组(数组(全局.B, 全局.C), 全局.A));
        "[B,C,D,E]"
        设置全局变量(A, 数组(全局.B, 全局.C, 全局.D, 全局.E));
    }
}

规则 ("other array functions") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 对全部为“真”(所有玩家(所有队伍), 比较((当前数组元素).A, ==, 2)));
        设置全局变量(A, 对任意为“真”(所有玩家(所有队伍), 比较((当前数组元素).A, ==, 2)));
        设置全局变量(A, 对任意为“真”(与(全局.A, 全局.B), 数组包含(全局.C, 当前数组元素)));
        设置全局变量(A, 对任意为“真”(If-Then-Else(全局.B, 全局.A, 全局.C), 比较(当前数组元素, ==, 2)));
        设置全局变量(A, 对全部为“真”(所有玩家(所有队伍), 当前数组元素));
        设置全局变量(A, 已排序的数组(全局.A, 加(当前数组元素, 2)));
        设置全局变量(A, 已排序的数组(全局.A, 当前数组元素));
        修改全局变量(A, 根据索引从数组中移除, 1);
        修改全局变量(A, 添加至数组, 3);
        修改全局变量(A, 根据值从数组中移除, 3);
        "Z,Y,X"
        设置全局变量(A, 数组(全局.Z, 全局.Y, 全局.X));
        设置全局变量(A, 已排序的数组(全局.B, 乘(-1, 当前数组索引)));
    }
}

规则 ("array to string") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 数组(10, 20, 30));
        设置全局变量(B, 数组(100));
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(全局.A, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("…"), 截取字符串(自定义字符串("…"), 加(-2, 数组中的值(当前数组元素, 1)), 减(4, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 0), 自定义字符串("+{0}", 除(数组中的值(当前数组元素, 1), 3)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(全局.A, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("{0}, …", 数组中的值(当前数组元素, 2)), 截取字符串(自定义字符串("0, …"), 加(-2, 数组中的值(当前数组元素, 1)), 减(7, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 3), 自定义字符串("+{0}", 减(除(数组中的值(当前数组元素, 1), 3), 1)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(全局.A, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("{0}, {1}, …", 数组中的值(当前数组元素, 2), 数组中的值(当前数组元素, 3)), 截取字符串(自定义字符串("0, 0, …"), 加(-2, 数组中的值(当前数组元素, 1)), 减(10, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 6), 自定义字符串("+{0}", 减(除(数组中的值(当前数组元素, 1), 3), 2)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(全局.A, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("{0}, {1}, {2}, …", 数组中的值(当前数组元素, 2), 数组中的值(当前数组元素, 3), 数组中的值(当前数组元素, 4)), 截取字符串(自定义字符串("0, 0, 0, …"), 加(-2, 数组中的值(当前数组元素, 1)), 减(13, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 9), 自定义字符串("+{0}", 减(除(数组中的值(当前数组元素, 1), 3), 3)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(全局.A, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("{0}, {1}, {2}", 数组中的值(当前数组元素, 2), 数组中的值(当前数组元素, 3), 自定义字符串("{0}, {1}, …", 数组中的值(当前数组元素, 4), 数组中的值(当前数组元素, 5))), 截取字符串(自定义字符串("0, 0, 0, 0, …"), 加(-2, 数组中的值(当前数组元素, 1)), 减(16, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 12), 自定义字符串("+{0}", 减(除(数组中的值(当前数组元素, 1), 3), 4)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(空数组, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("…"), 截取字符串(自定义字符串("…"), 加(-2, 数组中的值(当前数组元素, 1)), 减(4, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 0), 自定义字符串("+{0}", 除(数组中的值(当前数组元素, 1), 3)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(空数组, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("{0}, …", 数组中的值(当前数组元素, 2)), 截取字符串(自定义字符串("0, …"), 加(-2, 数组中的值(当前数组元素, 1)), 减(7, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 3), 自定义字符串("+{0}", 减(除(数组中的值(当前数组元素, 1), 3), 1)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(空数组, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("{0}, {1}, …", 数组中的值(当前数组元素, 2), 数组中的值(当前数组元素, 3)), 截取字符串(自定义字符串("0, 0, …"), 加(-2, 数组中的值(当前数组元素, 1)), 减(10, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 6), 自定义字符串("+{0}", 减(除(数组中的值(当前数组元素, 1), 3), 2)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(全局.B, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("…"), 截取字符串(自定义字符串("…"), 加(-2, 数组中的值(当前数组元素, 1)), 减(4, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 0), 自定义字符串("+{0}", 除(数组中的值(当前数组元素, 1), 3)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(全局.B, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("{0}, …", 数组中的值(当前数组元素, 2)), 截取字符串(自定义字符串("0, …"), 加(-2, 数组中的值(当前数组元素, 1)), 减(7, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 3), 自定义字符串("+{0}", 减(除(数组中的值(当前数组元素, 1), 3), 1)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 映射的数组(映射的数组(数组(映射的数组(全局.B, If-Then-Else(或(比较(数量(当前数组元素), ==, 1), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), 自定义字符串("[{0}]", 当前数组元素), If-Then-Else(数量(当前数组元素), 自定义字符串("[{0}, …+{1}]", 当前数组元素, 减(数量(当前数组元素), 1)), 当前数组元素)))), 添加至数组(添加至数组(或(数量(当前数组元素), 与(比较(当前数组元素, ==, 空数组), 比较(当前数组元素, !=, 空))), If-Then-Else(与(非(数量(当前数组元素)), 比较(当前数组元素, !=, 空数组)), 3, 乘(数量(当前数组元素), 3))), 当前数组元素)), If-Then-Else(首个(当前数组元素), 自定义字符串("[{0}{1}]", 字符串替换(自定义字符串("{0}, {1}, …", 数组中的值(当前数组元素, 2), 数组中的值(当前数组元素, 3)), 截取字符串(自定义字符串("0, 0, …"), 加(-2, 数组中的值(当前数组元素, 1)), 减(10, 数组中的值(当前数组元素, 1))), 空数组), If-Then-Else(比较(数组中的值(当前数组元素, 1), >, 6), 自定义字符串("+{0}", 减(除(数组中的值(当前数组元素, 1), 3), 2)), 空数组)), 字符串分割(数组中的值(当前数组元素, 2), 空数组))), 自定义字符串("                                                                                                                             {0}", 自定义字符串("                                             ")), 空, 左边, -9999, 颜色(橙色), 空, 空, 可见和字符串, 默认可见度);
    }
}

