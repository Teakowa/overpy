变量 {
    全局:
        0: A
        1: B
        2: C
        3: D
        4: E
}
//Optimize for size enabled
规则 ("switches") {
    事件 {
        持续 - 全局;
    }
    动作 {
        If(真);
        跳过(数组中的值(数组(4, 空, 2), 加(真, 数组值的索引(数组(1, 2), 全局.A))));
        //__label_switch_2_0__:
        修改全局变量(B, 加, 真);
        Else;
        //__label_switch_2_1__:
        修改全局变量(C, 加, 真);
        Else;
        //__label_switch_2_default__:
        End;
        If(真);
        跳过(数组中的值(数组(2, 空, 1, 1, 2), 加(真, 数组值的索引(数组(1, 2, 3, 英雄(布丽吉塔)), 全局.B))));
        //__label_switch_3_0__:
        修改全局变量(B, 加, 真);
        //__label_switch_3_1__:
        //__label_switch_3_2__:
        修改全局变量(C, 加, 真);
        //__label_switch_3_default__:
        //__label_switch_3_3__:
        修改全局变量(D, 加, 真);
        End;
    }
}

规则 ("nested switches") {
    事件 {
        持续 - 全局;
    }
    动作 {
        If(全局.A);
            If(真);
            跳过(数组中的值(数组(12, 空, 2, 4), 加(真, 数组值的索引(数组(1, 2, 3), 全局.B))));
            //__label_switch_5_0__:
            修改全局变量(C, 加, 真);
            Else;
            //__label_switch_5_1__:
            修改全局变量(D, 加, 真);
            Else;
            //__label_switch_5_2__:
            If(真);
            跳过(数组中的值(数组(4, 空, 3), 加(真, 数组值的索引(数组(1, 2), 全局.C))));
            //__label_switch_4_0__:
            修改全局变量(A, 加, 真);
            修改全局变量(B, 加, 真);
            Else;
            //__label_switch_4_1__:
            修改全局变量(C, 加, 真);
            //__label_switch_4_default__:
            修改全局变量(D, 加, 真);
            End;
            //__label_switch_5_default__:
            End;
        Else;
            修改全局变量(E, 加, 真);
    }
}

