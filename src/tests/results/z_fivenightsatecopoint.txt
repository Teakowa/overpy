设置
{
	主程序
	{
		描述: "Change nights through workshop settings (orange card)\nDMG46 / Release 1.0.4 (March 2024) / Alomare#11266"
		模式名称: "Five Nights at Ecopoint"
	}
	大厅
	{
		队列中的玩家可以加入: 是
		地图轮换: 暂停轮换
		比赛语音聊天: 启用
		观战者人数上限: 12
		玩家上限 队伍1: 1
		玩家上限 队伍2: 0
		返回大厅: 完成比赛后
	}
	模式
	{
		禁用 决斗先锋
		{
			目标点决胜机制: 关闭
		}
		团队死斗
		{
			启用地图
			{
				圣诞节生态监测站：南极洲
			}
			主动复生: 关闭
		}
		综合
		{
			游戏模式开始: 即时
			英雄限制: 关闭
			消灭提示: 关闭
			随机英雄复生: 开启
			复生时间: 0%
			急救包刷新: 禁用
		}
	}
	英雄
	{
		队伍1
		{
			美
			{
				急冻: 关闭
				冰墙: 关闭
				无需装弹: 开启
				主要攻击模式: 关闭
				辅助攻击模式: 关闭
				终极技能（暴雪）: 关闭
			}
			莱因哈特
			{
				冲锋冷却时间: 0%
			}
		}
		队伍2
		{
			重生时终极技能充能完毕: 开启
		}
		综合
		{
			启用英雄
			{
				美
			}
		}
	}
	扩展
	{
		光束效果
		光束声音
		增益状态效果
		减益状态效果
		增益效果和减益效果声音
		能量爆炸效果
		爆炸声音
		播放更多效果
	}
}
变量 {
    全局:
        0: NightNumber
        1: AutoRestart
        2: NoJumpscares
        3: Cheats
        4: Echo
        5: Orisa
        6: Genji
        7: Bastion
        8: Zen
        9: CamShake
        10: CurrentCam
        11: Player_
        12: LastCam
        13: InteractCams
        14: CamsJammed
        15: TextChatOn
        16: CurrentTime
        17: Power
        18: NightPhase
        19: ClosedFrontDoor
        20: InteractFrontDoor
        21: ClosedLeftDoor
        22: InteractLeftDoor
        23: ClosedRightDoor
        24: InteractRightDoor
        25: PowerUsage
        26: BastionPhase
        27: EchoPhase
        28: ZenDoorNumber
        29: Blinded
        30: PhoneRings
        31: CamPositions
        32: HeightFrontDoor
        33: HeightRightDoor
        34: HeightLeftDoor
        35: DoorsWalkable
        36: CamLookAts
        37: ZenAI
        38: BastionAI
        39: OrisaAI
        40: EchoAI
        41: GenjiAI
        42: OrisaMeter
        43: OrisaAIPrevious
        44: HeaterPower
        45: WarningIconVisible
        46: ZenPositions
        47: ZenPositions2
        48: ZenPickedCamAndPos
        49: BarValue
        50: BarTextAndColor
    玩家:
        0: HasMoved
}
子程序 {
    0: OutlineFix
    1: CameraShake
    2: CamsJammed
    3: PhoneRing
    4: BastionJumpscare
    5: EchoJumpscare
    6: OrisaJumpscare
    7: GenjiJumpscare
    8: ZenSequence
    9: ZenJumpscare
    10: Night6Call
    11: ZenDoor
    12: ZenMove
}
规则 ("[WS] Settings") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(NightNumber, 地图工坊设置组合(自定义字符串("GENERAL"), 自定义字符串("NIGHT NUMBER"), 0, 数组(自定义字符串("NIGHT 1"), 自定义字符串("NIGHT 2"), 自定义字符串("NIGHT 3"), 自定义字符串("NIGHT 4"), 自定义字符串("NIGHT 5"), 自定义字符串("NIGHT 6"), 自定义字符串("CUSTOM NIGHT")), 0));
        设置全局变量(AutoRestart, 地图工坊设置开关(自定义字符串("GENERAL"), 自定义字符串("AUTO-RESTART ON DEATH"), 假, 1));
        设置全局变量(NoJumpscares, 地图工坊设置开关(自定义字符串("GENERAL"), 自定义字符串("NO JUMPSCARES MODE"), 假, 2));
        "CHEATS"
        在索引处设置全局变量(Cheats, 0, 地图工坊设置开关(自定义字符串("CHEATS"), 自定义字符串("SHOW ENEMY INFO"), 假, 0));
        在索引处设置全局变量(Cheats, 1, 地图工坊设置开关(自定义字符串("CHEATS"), 自定义字符串("UNLIMITED POWER"), 假, 0));
    }
}

规则 ("OutlineFix") {
    事件 {
        子程序;
        OutlineFix;
    }
    动作 {
        传送(所有玩家(队伍2), 矢量(0, -1000, 0));
        击杀(所有玩家(队伍2), 空);
        等待(0.016, 无视条件);
        开始强制设置玩家轮廓(所有玩家(所有队伍), 所有玩家(所有队伍), 假, 空, 默认);
        等待(0.016, 无视条件);
        复活(所有玩家(队伍2));
        设置状态(全局.Echo, 空, 击倒, 99999);
        设置状态(全局.Orisa, 空, 击倒, 99999);
        设置状态(全局.Genji, 空, 击倒, 99999);
        传送(全局.Bastion, 矢量(-12.74, 7, -49.92));
        传送(全局.Echo, 矢量(28.62, 3, 2.9));
        传送(全局.Orisa, 矢量(-10.7, 8.52, -0.37));
        传送(全局.Genji, 矢量(-2.6, 7, 54.5));
        传送(全局.Zen, 矢量(2, 7.4, 22.4));
    }
}

规则 ("[SR] Camera Shake") {
    事件 {
        子程序;
        CameraShake;
    }
    动作 {
        设置全局变量(CamShake, 矢量(随机实数(-0.1, 0.1), 随机实数(-0.1, 0.1), 随机实数(-0.1, 0.1)));
        等待(0.016, 无视条件);
        循环;
    }
}

规则 ("[SR] Cams Jammed") {
    事件 {
        子程序;
        CamsJammed;
    }
    动作 {
        If(全局.CurrentCam);
            清除状态(全局.Player_, 定身);
            设置引力(全局.Player_, 100);
            设置全局变量(LastCam, 全局.CurrentCam);
            停止镜头(全局.Player_);
            设置朝向(全局.Player_, 方向(眼睛位置(全局.Player_), 全局.InteractCams), 至地图);
            设置全局变量(CurrentCam, 0);
        End;
        追踪全局变量频率(CamsJammed, 0, 1, 全部禁用);
        修改全局变量(CamsJammed, 加, 9);
        等待(0.05, 无视条件);
        播放效果(所有玩家(所有队伍), “黑百合”剧毒诡雷爆炸声音, 颜色(队伍1), 全局.InteractCams, 55);
        等待直到 (非(全局.CamsJammed), 99999);
        播放效果(所有玩家(所有队伍), 有害爆炸, 颜色(橙色), 全局.InteractCams, 0.25);
    }
}

规则 ("[EP] Toggle Text Chat") {
    事件 {
        持续 - 每名玩家;
        双方;
        全部;
    }
    条件 {
        游戏正在进行中 == 真;
        存活(事件玩家) == 真;
        按钮被按下(事件玩家, 按钮(终极技能)) == 真;
    }
    动作 {
        If(全局.TextChatOn);
            禁用文字聊天(事件玩家);
            小字体信息(所有玩家(所有队伍), 自定义字符串("TEXT CHAT DISABLED"));
            设置全局变量(TextChatOn, 0);
        Else;
            启用文字聊天(事件玩家);
            小字体信息(所有玩家(所有队伍), 自定义字符串("TEXT CHAT ENABLED"));
            设置全局变量(TextChatOn, 1);
        End;
        等待(0.5, 无视条件);
    }
}

规则 ("[G] Defeat") {
    事件 {
        玩家阵亡;
        双方;
        全部;
    }
    条件 {
        事件玩家 == 全局.Player_;
    }
    动作 {
        开启游戏预设音乐模式;
        宣告队伍胜利(队伍2);
        根据条件中止(非(全局.AutoRestart));
        等待(2, 无视条件);
        重新开始比赛;
    }
}

规则 ("[G] Victory") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.CurrentTime >= 6;
        全局.NightNumber != 5;
    }
    动作 {
        开启游戏预设音乐模式;
        宣告队伍胜利(队伍1);
    }
}

规则 ("[G] Power 0") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.Power <= 0;
        存活(全局.Player_) == 真;
        全局.NightPhase >= 3;
    }
    动作 {
        等待(0.1, 当为“假”时中止);
        播放效果(所有玩家(所有队伍), 卢西奥音障施放声音, 颜色(队伍1), 全局.InteractCams, 100);
        If(全局.ClosedFrontDoor);
            播放效果(所有玩家(所有队伍), “黑影”位移传动消失声音, 颜色(队伍2), 全局.InteractFrontDoor, 35);
            持续追踪全局变量(HeightFrontDoor, 0.01, 0.4, 全部禁用);
            设置全局变量(ClosedFrontDoor, 0);
        End;
        If(全局.ClosedLeftDoor);
            播放效果(所有玩家(所有队伍), “黑影”位移传动消失声音, 颜色(队伍2), 全局.InteractLeftDoor, 35);
            持续追踪全局变量(HeightLeftDoor, 0.01, 0.4, 全部禁用);
            设置全局变量(ClosedLeftDoor, 0);
        End;
        If(全局.ClosedRightDoor);
            播放效果(所有玩家(所有队伍), “黑影”位移传动消失声音, 颜色(队伍2), 全局.InteractRightDoor, 35);
            持续追踪全局变量(HeightRightDoor, 0.01, 0.4, 全部禁用);
            设置全局变量(ClosedRightDoor, 0);
        End;
        设置全局变量(PowerUsage, 0);
    }
}

规则 ("[G] Rein Charge") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        存活(此栏位的玩家(1, 队伍1)) == 真;
        或(对任意为“真”(数组(全局.BastionPhase, 全局.EchoPhase), 或(比较(当前数组元素, >=, 3), 比较(当前数组元素, <=, -3))), 全局.ZenDoorNumber) == 真;
        正在使用技能 1(此栏位的玩家(1, 队伍1)) == 假;
    }
    动作 {
        取消主要动作(此栏位的玩家(1, 队伍1));
        设置技能冷却(此栏位的玩家(1, 队伍1), 按钮(技能1), 0);
        按下按键(此栏位的玩家(1, 队伍1), 按钮(技能1));
        等待直到 (非(正在使用技能 1(此栏位的玩家(1, 队伍1))), 99999);
        如条件为“真”则循环;
    }
}

规则 ("[G] Main Setup") {
    事件 {
        持续 - 全局;
    }
    动作 {
        "REIN"
        生成机器人(英雄(莱因哈特), 队伍1, 1, 矢量(0, 200, 0), 右);
        开始强制设置玩家位置(此栏位的玩家(1, 队伍1), 矢量(0, 200, 0), 假);
        设置不可见(此栏位的玩家(1, 队伍1), 全部);
        "STUFF"
        关闭游戏预设通告模式;
        关闭游戏预设完成条件;
        关闭游戏预设音乐模式;
        关闭游戏预设计分模式;
        禁用查看器录制;
        设置全局变量(NightPhase, 999);
        等待直到 (与(游戏正在进行中, 已重生(此栏位的玩家(0, 队伍1))), 99999);
        开始规则(OutlineFix, 重新开始规则);
        比赛时间暂停;
        设置全局变量(Player_, 此栏位的玩家(0, 队伍1));
        设置全局变量(Power, 100);
        设置全局变量(PowerUsage, 1);
        持续追踪全局变量(CurrentTime, 6, 512, 全部禁用);
        创建效果(If-Then-Else(全局.Blinded, 全局.Player_, 空), 球体, 颜色(黑色), 全局.Player_, 100, 可见，位置和半径);
        传送(全局.Player_, 矢量(-1.06, 7.4, -27.55));
        设置朝向(全局.Player_, 矢量(0.7, 0, 0.7), 至地图);
        隐藏计分板(所有玩家(所有队伍));
        隐藏游戏模式HUD(所有玩家(所有队伍));
        隐藏英雄HUD(所有玩家(所有队伍));
        开始强制设置玩家轮廓(所有玩家(所有队伍), 所有玩家(所有队伍), 假, 空, 默认);
        隐藏姓名板(所有玩家(所有队伍), 所有玩家(所有队伍));
        禁用文字聊天(所有玩家(所有队伍));
        设置受到的击退(所有玩家(所有队伍), 0);
        设置造成伤害(全局.Player_, 0);
        "HUD"
        根据条件跳过(比较(全局.NightNumber, ==, 5), 1);
        创建HUD文本(所有玩家(所有队伍), 自定义字符串("{0} AM", If-Then-Else(比较(全局.CurrentTime, <, 1), 12, 取整(全局.CurrentTime, 下))), 自定义字符串("NIGHT {0}", 加(全局.NightNumber, 1)), If-Then-Else(对任意为“真”(全局.Cheats, 当前数组元素), 自定义字符串("CHEATS ENABLED"), 自定义字符串("")), 右边, 0, 颜色(白色), 颜色(白色), 颜色(青绿色), 可见和字符串, 默认可见度);
        //lbl_0:
        创建HUD文本(所有玩家(所有队伍), 自定义字符串("POWER: {0}%", 取整(全局.Power, 下)), 空, 空, 左边, 0, 颜色(白色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(所有玩家(所有队伍), 空, 自定义字符串(" \r\nUSAGE"), 截取字符串(自定义字符串("■■■■■"), 0, 全局.PowerUsage), 左边, 1, 颜色(白色), 颜色(白色), If-Then-Else(比较(全局.PowerUsage, <=, 2), 颜色(绿色), If-Then-Else(与(比较(全局.PowerUsage, >, 2), 比较(全局.PowerUsage, <=, 3)), 颜色(黄色), 颜色(红色))), 可见，字符串和颜色, 默认可见度);
        创建地图文本(If-Then-Else(全局.PhoneRings, 本地玩家, 空), 自定义字符串("HOLD [{0}] TO ANSWER PHONE OR WAIT TO IGNORE", 输入绑定字符串(按钮(技能1))), 逐帧更新(加(If-Then-Else(全局.CurrentCam, 数组中的值(全局.CamPositions, 全局.CurrentCam), 眼睛位置(全局.Player_)), 乘(与此角度的相对方向(水平朝向角度(全局.Player_), 加(垂直朝向角度(全局.Player_), 18)), 1000))), 1.5, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 200), 默认可见度);
        创建地图文本(If-Then-Else(全局.CurrentCam, 空, 全局.Player_), 自定义字符串("⊙"), 逐帧更新(加(眼睛位置(全局.Player_), 乘(与此角度的相对方向(水平朝向角度(全局.Player_), 加(垂直朝向角度(全局.Player_), 3.87)), 1000))), 2.3, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 170), 默认可见度);
    }
}

禁用 规则 ("DOORS") {
    事件 {
        持续 - 全局;
    }
}

规则 ("[G] Doors FX") {
    事件 {
        持续 - 全局;
    }
    动作 {
        等待(0.25, 无视条件);
        "Heights"
        设置全局变量(HeightFrontDoor, 0.01);
        设置全局变量(HeightRightDoor, 0.01);
        设置全局变量(HeightLeftDoor, 0.01);
        "Interact"
        设置全局变量(InteractFrontDoor, 矢量(6.99, 8.92, -22.78));
        设置全局变量(InteractRightDoor, 矢量(-1.8, 8.8, -23.51));
        设置全局变量(InteractLeftDoor, 矢量(0.7, 8.8, -30.66));
        创建效果(所有玩家(所有队伍), 球体, If-Then-Else(全局.ClosedFrontDoor, 颜色(红色), 颜色(灰色)), 全局.InteractFrontDoor, 0.15, 可见，位置，半径和颜色);
        创建效果(所有玩家(所有队伍), 球体, If-Then-Else(全局.ClosedRightDoor, 颜色(红色), 颜色(灰色)), 全局.InteractRightDoor, 0.15, 可见，位置，半径和颜色);
        创建效果(所有玩家(所有队伍), 球体, If-Then-Else(全局.ClosedLeftDoor, 颜色(红色), 颜色(灰色)), 全局.InteractLeftDoor, 0.15, 可见，位置，半径和颜色);
        等待(0.05, 无视条件);
        根据条件跳过(比较(全局.NightNumber, >=, 1), 4);
        创建地图文本(If-Then-Else(与(比较(全局.CurrentTime, <, 1), 非(全局.CurrentCam)), 所有玩家(所有队伍), 空), 自定义字符串("FRONT GATE"), 全局.InteractFrontDoor, 0.9, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 175), 默认可见度);
        创建地图文本(If-Then-Else(与(比较(全局.CurrentTime, <, 1), 非(全局.CurrentCam)), 所有玩家(所有队伍), 空), 自定义字符串("LEFT GATE"), 全局.InteractLeftDoor, 0.9, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 175), 默认可见度);
        创建地图文本(If-Then-Else(与(比较(全局.CurrentTime, <, 1), 非(全局.CurrentCam)), 所有玩家(所有队伍), 空), 自定义字符串("RIGHT GATE"), 全局.InteractRightDoor, 0.9, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 175), 默认可见度);
        等待(0.05, 无视条件);
        //lbl_0:
        创建效果(If-Then-Else(与(比较(相距距离(眼睛位置(全局.Player_), 全局.InteractFrontDoor), <, 2), 比较(矢量间夹角(面朝方向(全局.Player_), 方向(眼睛位置(全局.Player_), 全局.InteractFrontDoor)), <, 30)), 所有玩家(所有队伍), 空), 球体, 颜色(白色), 全局.InteractFrontDoor, 0.065, 可见，位置，半径和颜色);
        创建效果(If-Then-Else(与(比较(相距距离(眼睛位置(全局.Player_), 全局.InteractRightDoor), <, 2), 比较(矢量间夹角(面朝方向(全局.Player_), 方向(眼睛位置(全局.Player_), 全局.InteractRightDoor)), <, 30)), 所有玩家(所有队伍), 空), 球体, 颜色(白色), 全局.InteractRightDoor, 0.065, 可见，位置，半径和颜色);
        创建效果(If-Then-Else(与(比较(相距距离(眼睛位置(全局.Player_), 全局.InteractLeftDoor), <, 2), 比较(矢量间夹角(面朝方向(全局.Player_), 方向(眼睛位置(全局.Player_), 全局.InteractLeftDoor)), <, 30)), 所有玩家(所有队伍), 空), 球体, 颜色(白色), 全局.InteractLeftDoor, 0.065, 可见，位置，半径和颜色);
        等待(0.05, 无视条件);
        "FRONT BEAMS"
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(5.96, 10.88, -20.88), 矢量(5.96, 加(10.88, 全局.HeightFrontDoor), -20.88), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(5.64, 10.88, -20.57), 矢量(5.64, 加(10.88, 全局.HeightFrontDoor), -20.57), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(5.33, 10.88, -20.26), 矢量(5.33, 加(10.88, 全局.HeightFrontDoor), -20.26), 颜色(白色), 可见，位置和半径);
        等待(0.05, 无视条件);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(6.27, 10.88, -21.19), 矢量(6.27, 加(10.88, 全局.HeightFrontDoor), -21.19), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(6.57, 10.88, -21.5), 矢量(6.57, 加(10.88, 全局.HeightFrontDoor), -21.5), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), 抓钩光束, 矢量(6.81, 加(10.88, 全局.HeightFrontDoor), -21.74), 矢量(5.09, 加(10.88, 全局.HeightFrontDoor), -20.02), 颜色(白色), 可见，位置和半径);
        等待(0.05, 无视条件);
        "LEFT BEAMS"
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(2.48, 10.88, -30.14), 矢量(2.48, 加(10.88, 全局.HeightLeftDoor), -30.14), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(2.79, 10.88, -29.83), 矢量(2.79, 加(10.88, 全局.HeightLeftDoor), -29.83), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(3.1, 10.88, -29.52), 矢量(3.1, 加(10.88, 全局.HeightLeftDoor), -29.52), 颜色(白色), 可见，位置和半径);
        等待(0.05, 无视条件);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(2.17, 10.88, -30.45), 矢量(2.17, 加(10.88, 全局.HeightLeftDoor), -30.45), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(1.86, 10.88, -30.76), 矢量(1.86, 加(10.88, 全局.HeightLeftDoor), -30.76), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), 抓钩光束, 矢量(1.64, 加(10.88, 全局.HeightLeftDoor), -31), 矢量(3.33, 加(10.88, 全局.HeightLeftDoor), -29.29), 颜色(白色), 可见，位置和半径);
        等待(0.05, 无视条件);
        "RIGHT BEAMS"
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(-3.3, 10.88, -24.36), 矢量(-3.3, 加(10.88, 全局.HeightRightDoor), -24.36), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(-3.61, 10.88, -24.67), 矢量(-3.61, 加(10.88, 全局.HeightRightDoor), -24.67), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(-3.92, 10.88, -24.98), 矢量(-3.92, 加(10.88, 全局.HeightRightDoor), -24.98), 颜色(白色), 可见，位置和半径);
        等待(0.05, 无视条件);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(-2.99, 10.88, -24.05), 矢量(-2.99, 加(10.88, 全局.HeightRightDoor), -24.05), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), “秩序之光”光子发射器光束, 矢量(-2.68, 10.88, -23.74), 矢量(-2.68, 加(10.88, 全局.HeightRightDoor), -23.74), 颜色(白色), 可见，位置和半径);
        创建光束效果(所有玩家(所有队伍), 抓钩光束, 矢量(-2.45, 加(10.88, 全局.HeightRightDoor), -23.51), 矢量(-4.15, 加(10.88, 全局.HeightRightDoor), -25.21), 颜色(白色), 可见，位置和半径);
    }
}

规则 ("[G] Front Door Interact") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        相距距离(眼睛位置(全局.Player_), 全局.InteractFrontDoor) < 2;
        矢量间夹角(面朝方向(全局.Player_), 方向(眼睛位置(全局.Player_), 全局.InteractFrontDoor)) < 30;
        按钮被按下(全局.Player_, 按钮(互动)) == 真;
        在视线内(眼睛位置(全局.Player_), 全局.InteractFrontDoor, 屏障不会阻挡视线) == 真;
    }
    动作 {
        If(全局.Power);
            If(全局.ClosedFrontDoor);
                播放效果(所有玩家(所有队伍), “黑影”位移传动消失声音, 颜色(队伍2), 全局.InteractFrontDoor, 35);
                持续追踪全局变量(HeightFrontDoor, 0.01, 0.4, 全部禁用);
                设置全局变量(ClosedFrontDoor, 0);
                修改全局变量(PowerUsage, 减, 1);
                等待(0.4, 无视条件);
            Else;
                播放效果(所有玩家(所有队伍), “黑影”位移传动重现声音, 颜色(队伍2), 全局.InteractFrontDoor, 50);
                持续追踪全局变量(HeightFrontDoor, -3.5, 0.4, 全部禁用);
                设置全局变量(ClosedFrontDoor, 1);
                修改全局变量(PowerUsage, 加, 1);
                等待(0.4, 无视条件);
                根据条件中止(非(全局.Power));
                播放效果(所有玩家(所有队伍), “破坏球”重力坠击击中声音, 颜色(队伍2), 全局.InteractFrontDoor, 8);
            End;
        Else;
            播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 全局.InteractFrontDoor, 125);
    }
}

规则 ("[G] Left Door Interact") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        相距距离(眼睛位置(全局.Player_), 全局.InteractLeftDoor) < 2;
        矢量间夹角(面朝方向(全局.Player_), 方向(眼睛位置(全局.Player_), 全局.InteractLeftDoor)) < 30;
        按钮被按下(全局.Player_, 按钮(互动)) == 真;
        在视线内(眼睛位置(全局.Player_), 全局.InteractLeftDoor, 屏障不会阻挡视线) == 真;
    }
    动作 {
        If(全局.Power);
            If(全局.ClosedLeftDoor);
                播放效果(所有玩家(所有队伍), “黑影”位移传动消失声音, 颜色(队伍2), 全局.InteractLeftDoor, 35);
                持续追踪全局变量(HeightLeftDoor, 0.01, 0.4, 全部禁用);
                设置全局变量(ClosedLeftDoor, 0);
                修改全局变量(PowerUsage, 减, 1);
                等待(0.4, 无视条件);
            Else;
                播放效果(所有玩家(所有队伍), “黑影”位移传动重现声音, 颜色(队伍2), 全局.InteractLeftDoor, 50);
                持续追踪全局变量(HeightLeftDoor, -3.5, 0.4, 全部禁用);
                设置全局变量(ClosedLeftDoor, 1);
                修改全局变量(PowerUsage, 加, 1);
                等待(0.4, 无视条件);
                根据条件中止(非(全局.Power));
                播放效果(所有玩家(所有队伍), “破坏球”重力坠击击中声音, 颜色(队伍2), 全局.InteractLeftDoor, 8);
            End;
        Else;
            播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 全局.InteractLeftDoor, 125);
    }
}

规则 ("[G] Right Door Interact") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        相距距离(眼睛位置(全局.Player_), 全局.InteractRightDoor) < 2;
        矢量间夹角(面朝方向(全局.Player_), 方向(眼睛位置(全局.Player_), 全局.InteractRightDoor)) < 30;
        按钮被按下(全局.Player_, 按钮(互动)) == 真;
        在视线内(眼睛位置(全局.Player_), 全局.InteractRightDoor, 屏障不会阻挡视线) == 真;
    }
    动作 {
        If(全局.Power);
            If(全局.ClosedRightDoor);
                播放效果(所有玩家(所有队伍), “黑影”位移传动消失声音, 颜色(队伍2), 全局.InteractRightDoor, 35);
                持续追踪全局变量(HeightRightDoor, 0.01, 0.4, 全部禁用);
                设置全局变量(ClosedRightDoor, 0);
                修改全局变量(PowerUsage, 减, 1);
                等待(0.4, 无视条件);
            Else;
                播放效果(所有玩家(所有队伍), “黑影”位移传动重现声音, 颜色(队伍2), 全局.InteractRightDoor, 50);
                持续追踪全局变量(HeightRightDoor, -3.5, 0.4, 全部禁用);
                设置全局变量(ClosedRightDoor, 1);
                修改全局变量(PowerUsage, 加, 1);
                等待(0.4, 无视条件);
                根据条件中止(非(全局.Power));
                播放效果(所有玩家(所有队伍), “破坏球”重力坠击击中声音, 颜色(队伍2), 全局.InteractRightDoor, 8);
            End;
        Else;
            播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 全局.InteractRightDoor, 125);
    }
}

规则 ("[G] Front Door Barrier") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        相距距离(所选位置(全局.Player_), 矢量(8.21, 7.38, -18.62)) < 4;
        或(非(全局.DoorsWalkable), 全局.ClosedFrontDoor) == 真;
    }
    动作 {
        施加推力(全局.Player_, 方向(矢量(8.43, 7.38, -18.4), 所选位置(全局.Player_)), 4, 至地图, 取消相反运动);
        等待(0.02, 无视条件);
        如条件为“真”则循环;
    }
}

规则 ("[G] Left Door Barrier") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        相距距离(所选位置(全局.Player_), 矢量(4.81, 7.38, -32.44)) < 4;
        或(非(全局.DoorsWalkable), 全局.ClosedLeftDoor) == 真;
    }
    动作 {
        施加推力(全局.Player_, 方向(矢量(5.02, 7.38, -32.66), 所选位置(全局.Player_)), 4, 至地图, 取消相反运动);
        等待(0.02, 无视条件);
        如条件为“真”则循环;
    }
}

规则 ("[G] Right Door Barrier") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        相距距离(所选位置(全局.Player_), 矢量(-5.65, 7.38, -22.06)) < 4;
        或(非(全局.DoorsWalkable), 全局.ClosedRightDoor) == 真;
    }
    动作 {
        施加推力(全局.Player_, 方向(矢量(-5.87, 7.38, -21.84), 所选位置(全局.Player_)), 4, 至地图, 取消相反运动);
        等待(0.02, 无视条件);
        如条件为“真”则循环;
    }
}

禁用 规则 ("CAMERAS") {
    事件 {
        持续 - 全局;
    }
}

规则 ("[G] Cams Setup") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(CamPositions, 数组(空, 矢量(1.367, 9.276, -54.926), 矢量(-5.5, 10, -27), 矢量(-10.08, 9.75, -8.96), 矢量(20.45, 3.38, 1.08), 矢量(18, 7.1, -6.6), 矢量(21.7, 11.51, -8.91), 矢量(-3.8, 10.4, 52.8)));
        设置全局变量(CamLookAts, 数组(空, 矢量(-0.97, -0.1, 0.2), 矢量(-0.7, -0.25, -0.7), 矢量(-0.4, -0.3, 0.87), 左, 矢量(0, -0.3, -1), 矢量(0.6, -0.15, 0.79), 矢量(-0.18, -0.78, 0.6)));
        设置全局变量(InteractCams, 矢量(0.55, 8.4, -22.2));
        设置全局变量(CurrentCam, 0);
        设置全局变量(LastCam, 1);
        根据条件跳过(比较(全局.NightNumber, >=, 1), 1);
        创建地图文本(If-Then-Else(与(比较(全局.CurrentTime, <, 1), 非(全局.CurrentCam)), 本地玩家, 空), 自定义字符串("[{0}] TO ENTER/LEAVE CAMERAS", 输入绑定字符串(按钮(互动))), 全局.InteractCams, 1, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 200), 默认可见度);
        //lbl_0:
        创建HUD文本(If-Then-Else(全局.CurrentCam, 所有玩家(所有队伍), 空), 自定义字符串("CAM {0}", 全局.CurrentCam), 空, 空, 顶部, 0, 颜色(白色), 空, 空, 可见和字符串, 默认可见度);
        创建HUD文本(If-Then-Else(全局.CurrentCam, 本地玩家, 空), 空, 自定义字符串("<  {0}  |  {1}  >", 输入绑定字符串(按钮(辅助攻击模式)), 输入绑定字符串(按钮(主要攻击模式))), 空, 顶部, 1, 空, 颜色(白色), 空, 可见和字符串, 默认可见度);
        创建效果(If-Then-Else(与(比较(相距距离(眼睛位置(全局.Player_), 全局.InteractCams), <, 2), 比较(矢量间夹角(面朝方向(全局.Player_), 方向(眼睛位置(全局.Player_), 全局.InteractCams)), <, 30)), 所有玩家(所有队伍), 空), 球体, 颜色(白色), 全局.InteractCams, 0.065, 可见，位置，半径和颜色);
        创建效果(If-Then-Else(全局.CamsJammed, 所有玩家(所有队伍), 空), 温斯顿原始暴怒效果, 颜色(队伍1), 全局.InteractCams, 1, 可见，位置和半径);
        创建效果(所有玩家(所有队伍), 环, If-Then-Else(全局.CamsJammed, 颜色(橙色), 颜色(灰色)), 全局.InteractCams, 0.1, 可见，位置，半径和颜色);
        等待(0.1, 无视条件);
        创建效果(所有玩家(所有队伍), 球体, 颜色(黑色), 数组中的值(全局.CamPositions, 1), 0.25, 可见);
        创建效果(所有玩家(所有队伍), 球体, 颜色(黑色), 数组中的值(全局.CamPositions, 2), 0.25, 可见);
        创建效果(所有玩家(所有队伍), 球体, 颜色(黑色), 数组中的值(全局.CamPositions, 3), 0.25, 可见);
        创建效果(所有玩家(所有队伍), 球体, 颜色(黑色), 数组中的值(全局.CamPositions, 4), 0.25, 可见);
        创建效果(所有玩家(所有队伍), 球体, 颜色(黑色), 数组中的值(全局.CamPositions, 5), 0.25, 可见);
        创建效果(所有玩家(所有队伍), 球体, 颜色(黑色), 数组中的值(全局.CamPositions, 6), 0.25, 可见);
        创建效果(所有玩家(所有队伍), 球体, 颜色(黑色), 数组中的值(全局.CamPositions, 7), 0.25, 可见);
    }
}

规则 ("[EP] Cams Interact") {
    事件 {
        持续 - 每名玩家;
        双方;
        全部;
    }
    条件 {
        游戏正在进行中 == 真;
        相距距离(眼睛位置(事件玩家), 全局.InteractCams) < 2;
        按钮被按下(事件玩家, 按钮(互动)) == 真;
        正在移动(事件玩家) == 假;
        是否是机器人(事件玩家) == 假;
    }
    动作 {
        If(与(非(全局.CamsJammed), 全局.Power));
            If(比较(全局.CurrentCam, ==, 0));
                根据条件中止(比较(矢量间夹角(面朝方向(事件玩家), 方向(眼睛位置(事件玩家), 全局.InteractCams)), >=, 30));
                设置全局变量(CurrentCam, 全局.LastCam);
                修改全局变量(PowerUsage, 加, 1);
                设置状态(事件玩家, 空, 定身, 9999);
                设置引力(事件玩家, 0);
                设置启用近战攻击(事件玩家, 假);
                开始镜头(事件玩家, 数组中的值(全局.CamPositions, 全局.CurrentCam), 逐帧更新(加(数组中的值(全局.CamPositions, 全局.CurrentCam), 面朝方向(事件玩家))), 0);
                设置朝向(事件玩家, 数组中的值(全局.CamLookAts, 全局.CurrentCam), 至地图);
                等待(0.05, 无视条件);
                播放效果(所有玩家(所有队伍), “黑影”位移传动声音, 颜色(队伍2), 数组中的值(全局.CamPositions, 全局.CurrentCam), 45);
            Else;
                设置全局变量(CurrentCam, 0);
                修改全局变量(PowerUsage, 减, 1);
                清除状态(事件玩家, 定身);
                设置引力(事件玩家, 100);
                设置启用近战攻击(事件玩家, 真);
                停止镜头(事件玩家);
                设置朝向(事件玩家, 方向(眼睛位置(事件玩家), 全局.InteractCams), 至地图);
            End;
            等待(0.05, 无视条件);
            播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍2), 所选位置(事件玩家), 200);
        Else;
            播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 全局.InteractCams, 125);
    }
}

规则 ("[EP] Cycle Cams") {
    事件 {
        持续 - 每名玩家;
        双方;
        全部;
    }
    条件 {
        或(按钮被按下(事件玩家, 按钮(主要攻击模式)), 按钮被按下(事件玩家, 按钮(辅助攻击模式))) == 真;
        按钮被按下(事件玩家, 按钮(装填)) == 假;
        全局.CurrentCam != 0;
        "@Condition Power > 0"
        是否是机器人(事件玩家) == 假;
    }
    动作 {
        If(按钮被按下(事件玩家, 按钮(主要攻击模式)));
            If(比较(全局.CurrentCam, >=, 减(数量(全局.CamPositions), 1)));
                设置全局变量(CurrentCam, 1);
            Else;
                修改全局变量(CurrentCam, 加, 1);
            End;
        Else If(按钮被按下(事件玩家, 按钮(辅助攻击模式)));
            If(比较(全局.CurrentCam, <=, 1));
                设置全局变量(CurrentCam, 减(数量(全局.CamPositions), 1));
            Else;
                修改全局变量(CurrentCam, 减, 1);
            End;
        End;
        设置朝向(事件玩家, 数组中的值(全局.CamLookAts, 全局.CurrentCam), 至地图);
        等待(0.05, 无视条件);
        播放效果(所有玩家(所有队伍), “黑影”位移传动声音, 颜色(队伍1), 数组中的值(全局.CamPositions, 全局.CurrentCam), 45);
    }
}

规则 ("[G] Update Last Cam / Zen Chance") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.CurrentCam != 0;
        全局.LastCam != 全局.CurrentCam;
    }
    动作 {
        设置全局变量(LastCam, 全局.CurrentCam);
        根据条件中止(或(比较(随机整数(1, 100), >, 全局.ZenAI), 比较(全局.NightNumber, ==, 5)));
        开始规则(ZenSequence, 无动作);
    }
}

禁用 规则 ("NIGHTS") {
    事件 {
        持续 - 全局;
    }
}

规则 ("[SR] Phone Ring") {
    事件 {
        子程序;
        PhoneRing;
    }
    动作 {
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
        等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 0.08);
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 空, 全局.InteractCams, 8);
    }
}

规则 ("[G] Night 1") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 0;
        游戏正在进行中 == 真;
    }
    动作 {
        根据条件跳过(数组中的值(全局.Cheats, 1), 1);
        追踪全局变量频率(Power, 0, 乘(0.083, 全局.PowerUsage), 速率及最终值);
        //lbl_0:
        大字体信息(所有玩家(所有队伍), 自定义字符串("SURVIVE UNTIL 6 AM"));
        等待直到 (比较(全局.CurrentTime, >=, 2), 99999);
        修改全局变量(BastionAI, 加, 3);
        等待直到 (比较(全局.CurrentTime, >=, 3), 99999);
        根据条件跳过(比较(随机整数(1, 100), >, 10), 1);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[?] IT SHOULD HAVE BEEN YOU."));
        //lbl_1:
        修改全局变量(BastionAI, 加, 1);
        修改全局变量(OrisaAI, 加, 3);
    }
}

规则 ("[G] Night 1 Call") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 0;
        游戏正在进行中 == 真;
        存活(全局.Player_) == 真;
    }
    动作 {
        等待(0.5, 无视条件);
        设置全局变量(PhoneRings, 4);
        While(与(全局.PhoneRings, 非(按钮被按下(全局.Player_, 按钮(技能1)))));
            调用子程序(PhoneRing);
            修改全局变量(PhoneRings, 减, 1);
            等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 2);
        End;
        根据条件中止(非(按钮被按下(全局.Player_, 按钮(技能1))));
        设置全局变量(PhoneRings, 0);
        等待(0.5, 无视条件);
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 200);
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] UHM... HELLO? MEI?"));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AH, THANK GOD COMMS ARE BACK."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] YOU SHOULD RECEIVE THIS BY TOMORROW NIGHT..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] WHAT A TRAGEDY. I'M REALLY SORRY ABOUT YOUR FRIENDS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THESE NINE YEARS HAVE BEEN ROUGH, BUT IT'S GOOD TO HAVE YOU BACK."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] ANYWAY, THERE'S NO TIME TO WASTE."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THE NEW CREW IS GETTING READY..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] BUT THE STORM HAS SEVERELY DAMAGED"));
        等待(1.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("MOST OF THE EQUIPMENT IN THERE..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] INCLUDING SECURITY SYSTEMS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] SO WE'VE ASSIGNED YOU TO GET FAMILIAR"));
        等待(1.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("WITH OUR - VERY EXPENSIVE - ROBOTS..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AND MAKE SURE THEY'RE IN GOOD CONDITIONS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] SO... YEAH. JUST KEEP THEM SAFE AND WORKING."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AND REPORT ME ANYTHING STRANGE YOU FIND."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] WE'LL TALK MORE TOMORROW. GOOD LUCK!"));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX HANGS UP]"));
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 130);
    }
}

规则 ("[G] Night 2") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 1;
        游戏正在进行中 == 真;
    }
    动作 {
        根据条件跳过(数组中的值(全局.Cheats, 1), 1);
        追踪全局变量频率(Power, 0, 乘(0.1, 全局.PowerUsage), 速率及最终值);
        //lbl_0:
        大字体信息(所有玩家(所有队伍), 自定义字符串("SURVIVE UNTIL 6 AM"));
        修改全局变量(ZenAI, 加, 1);
        修改全局变量(BastionAI, 加, 2);
        等待直到 (比较(全局.CurrentTime, >=, 1), 99999);
        修改全局变量(BastionAI, 加, 2);
        修改全局变量(OrisaAI, 加, 3);
        等待直到 (比较(全局.CurrentTime, >=, 2), 99999);
        根据条件跳过(比较(随机整数(1, 100), >, 10), 1);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[?] IT'S ALL YOUR FAULT."));
        //lbl_1:
        修改全局变量(EchoAI, 加, 2);
        等待直到 (比较(全局.CurrentTime, >=, 3), 99999);
        修改全局变量(BastionAI, 加, 2);
        等待直到 (比较(全局.CurrentTime, >=, 4), 99999);
        修改全局变量(EchoAI, 加, 1);
    }
}

规则 ("[G] Night 2 Call") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 1;
        游戏正在进行中 == 真;
        存活(全局.Player_) == 真;
    }
    动作 {
        等待(0.5, 无视条件);
        设置全局变量(PhoneRings, 4);
        While(与(全局.PhoneRings, 非(按钮被按下(全局.Player_, 按钮(技能1)))));
            调用子程序(PhoneRing);
            修改全局变量(PhoneRings, 减, 1);
            等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 2);
        End;
        根据条件中止(非(按钮被按下(全局.Player_, 按钮(技能1))));
        设置全局变量(PhoneRings, 0);
        等待(0.5, 无视条件);
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 200);
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] MEI! WHAT'S UP?"));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] HOPE YOU'RE DOING FINE OUT THERE."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] I HAVE SOME NEW INFO BASED ON YOUR REPORTS..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AND I GOTTA FILL YOU UP RIGHT NOW."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] SO... THERE'S SOMETHING ABOUT ECHO."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] YOU MUST HAVE SEEN HER SOMEWHERE ALREADY."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] SHE IS... SMARTER THAN THE OTHER ONES..."));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("IN A CERTAIN WAY."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] SHE HAS REMOTE ACCESS TO THE POWER NETWORK..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AND SHE IS MALFUNCTIONING RIGHT NOW."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] BUT NO WORRIES!"));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] IF SHE TRIES TO PLAY ANY TRICKS ON YOU..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] JUST CLOSE THE GATE AND HIDE FROM HER VIEW FOR A BIT."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THAT UH... SHOULD MAKE HER GO AWAY FOR A WHILE."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] I THINK THAT'S IT. GOOD LUCK AGAIN!"));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AND KEEP SENDING THESE REPORTS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THEY'RE VERY HELPFUL."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] PEACE!"));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX HANGS UP]"));
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 130);
    }
}

规则 ("[G] Night 3") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 2;
        游戏正在进行中 == 真;
    }
    动作 {
        设置全局变量(NightPhase, 999);
        根据条件跳过(数组中的值(全局.Cheats, 1), 1);
        追踪全局变量频率(Power, 0, 乘(0.116, 全局.PowerUsage), 速率及最终值);
        //lbl_0:
        大字体信息(所有玩家(所有队伍), 自定义字符串("SURVIVE UNTIL 6 AM"));
        修改全局变量(ZenAI, 加, 1);
        修改全局变量(GenjiAI, 加, 2);
        修改全局变量(BastionAI, 加, 3);
        修改全局变量(OrisaAI, 加, 3);
        等待直到 (比较(全局.CurrentTime, >=, 1), 99999);
        修改全局变量(EchoAI, 加, 4);
        等待直到 (比较(全局.CurrentTime, >=, 2), 99999);
        修改全局变量(BastionAI, 加, 2);
        修改全局变量(OrisaAI, 加, 1);
        等待直到 (比较(全局.CurrentTime, >=, 3), 99999);
        修改全局变量(EchoAI, 加, 3);
        等待直到 (比较(全局.CurrentTime, >=, 4), 99999);
        根据条件跳过(比较(随机整数(1, 100), >, 10), 1);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[?] IT'S YOUR TURN TO FREEZE."));
        //lbl_1:
        修改全局变量(OrisaAI, 加, 2);
    }
}

规则 ("[G] Night 3 Call") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 2;
        游戏正在进行中 == 真;
        存活(全局.Player_) == 真;
    }
    动作 {
        等待(0.5, 无视条件);
        设置全局变量(PhoneRings, 4);
        While(与(全局.PhoneRings, 非(按钮被按下(全局.Player_, 按钮(技能1)))));
            调用子程序(PhoneRing);
            修改全局变量(PhoneRings, 减, 1);
            等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 2);
        End;
        根据条件中止(非(按钮被按下(全局.Player_, 按钮(技能1))));
        设置全局变量(PhoneRings, 0);
        等待(0.5, 无视条件);
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 200);
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] HELLO HELLO! READY FOR YOUR THIRD NIGHT?"));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] WE HAVE BEEN REEEALLY BUSY WITH"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("ALL THE READINGS YOU PROVIDED ME."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THERE'S ANOTHER DETAIL YOU MUST KNOW."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THE OLD HUNTER BOT PROTOTYPE..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THAT WAS BEING KEPT IN ECOPOINT FOR REASONS"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("EVEN I DON'T KNOW..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] HAS BOOTED UP SOMEHOW."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] HE USES THERMAL SENSORS TO FIND AND NEUTRALIZE..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] ANYTHING THAT'S DOING SOMETHING IT IS"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("PROGRAMMED TO PREVENT."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AND SINCE THE FACILITY IS IN LOCKDOWN..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AND THE ONLY HEAT SIGNATURE AROUND IS YOU..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] WELL, I THINK YOU KNOW WHAT I MEAN."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] BUT DON'T PANIC. WE'VE PLAYED AROUND"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("WITH THE HEAT SYSTEM FOR A BIT..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] TO MESS WITH HIS SENSORS..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AND LOCK HIM IN THE CORRIDOR NEXT TO"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("THE CREW QUARTERS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] SO YOU SHOULD BE SAFE..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] AS LONG AS YOU KEEP THAT HEATER POWERED."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] I'M REALLY SORRY ABOUT THIS MEI."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] WE'RE WORKING HARD TO FIX EVERYTHING."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THIS DAMN STORM..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] BUT YOU'LL BE FINE. I PROMISE."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THANK YOU FOR DOING THIS FOR US."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX HANGS UP]"));
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 130);
    }
}

规则 ("[G] Night 4") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 3;
        游戏正在进行中 == 真;
    }
    动作 {
        根据条件跳过(数组中的值(全局.Cheats, 1), 1);
        追踪全局变量频率(Power, 0, 乘(0.12, 全局.PowerUsage), 速率及最终值);
        //lbl_0:
        大字体信息(所有玩家(所有队伍), 自定义字符串("SURVIVE UNTIL 6 AM"));
        修改全局变量(ZenAI, 加, 1);
        修改全局变量(GenjiAI, 加, 3);
        修改全局变量(OrisaAI, 加, 3);
        修改全局变量(BastionAI, 加, 4);
        修改全局变量(EchoAI, 加, 4);
        等待直到 (比较(全局.CurrentTime, >=, 2), 99999);
        修改全局变量(BastionAI, 加, 4);
        修改全局变量(EchoAI, 加, 4);
        等待直到 (比较(全局.CurrentTime, >=, 4), 99999);
        修改全局变量(OrisaAI, 加, 3);
        等待直到 (比较(全局.CurrentTime, >=, 5), 99999);
        修改全局变量(GenjiAI, 加, 9);
    }
}

规则 ("[G] Night 4 Call") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 3;
        游戏正在进行中 == 真;
        存活(全局.Player_) == 真;
    }
    动作 {
        等待(0.5, 无视条件);
        设置全局变量(PhoneRings, 4);
        While(与(全局.PhoneRings, 非(按钮被按下(全局.Player_, 按钮(技能1)))));
            调用子程序(PhoneRing);
            修改全局变量(PhoneRings, 减, 1);
            等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 2);
        End;
        根据条件中止(非(按钮被按下(全局.Player_, 按钮(技能1))));
        设置全局变量(PhoneRings, 0);
        等待(0.5, 无视条件);
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 200);
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[STATIC]"));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[?] YOU SHOULD HAVE DIED WITH US."));
        等待(2.5, 无视条件);
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 130);
    }
}

规则 ("[G] Night 5") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 4;
        游戏正在进行中 == 真;
    }
    动作 {
        根据条件跳过(数组中的值(全局.Cheats, 1), 1);
        追踪全局变量频率(Power, 0, 乘(0.125, 全局.PowerUsage), 速率及最终值);
        //lbl_0:
        大字体信息(所有玩家(所有队伍), 自定义字符串("SURVIVE UNTIL 6 AM"));
        修改全局变量(ZenAI, 加, 15);
        修改全局变量(GenjiAI, 加, 7);
        修改全局变量(OrisaAI, 加, 6);
        修改全局变量(BastionAI, 加, 6);
        修改全局变量(EchoAI, 加, 5);
        等待直到 (比较(全局.CurrentTime, >=, 2), 99999);
        修改全局变量(OrisaAI, 加, 3);
        修改全局变量(BastionAI, 加, 2);
        修改全局变量(EchoAI, 加, 3);
        等待直到 (比较(全局.CurrentTime, >=, 4), 99999);
        根据条件跳过(比较(随机整数(1, 100), >, 10), 1);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[?] YOU LEFT US BEHIND."));
        //lbl_1:
        修改全局变量(GenjiAI, 加, 3);
        修改全局变量(OrisaAI, 加, 3);
        修改全局变量(BastionAI, 加, 3);
        修改全局变量(EchoAI, 加, 3);
    }
}

规则 ("[G] Night 5 Call") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 4;
        游戏正在进行中 == 真;
        存活(全局.Player_) == 真;
    }
    动作 {
        等待(0.5, 无视条件);
        设置全局变量(PhoneRings, 4);
        While(与(全局.PhoneRings, 非(按钮被按下(全局.Player_, 按钮(技能1)))));
            调用子程序(PhoneRing);
            修改全局变量(PhoneRings, 减, 1);
            等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 2);
        End;
        根据条件中止(非(按钮被按下(全局.Player_, 按钮(技能1))));
        设置全局变量(PhoneRings, 0);
        等待(0.5, 无视条件);
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 200);
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] MEI? I HOPE THIS MESSAGE REACHES YOU."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] YOUR SIGNAL WENT DOWN YESTERDAY."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] IF YOU'RE LISTENING TO ME, PLEASE PAY ATTENTION."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] WE'VE UNCOVERED PROJECTS FOR A FIFTH SECURITY BOT."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] WE DO NOT KNOW WHERE IT IS AND HOW IT WORKS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] THE NEW CREW WON'T MOVE TO ECOPOINT UNTIL"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("THIS IS ALL SORTED OUT AND CLARIFIED."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] GOD KNOWS WHY THE HELL YOU AND YOUR"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("COLLEAGUES WERE LIVING NEXT TO THESE THINGS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX] PLEASE BE CAREFUL. HELP IS ON THE WAY."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MAX HANGS UP]"));
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 130);
    }
}

规则 ("[G] Night 7 (Custom)") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber == 6;
        游戏正在进行中 == 真;
    }
    动作 {
        根据条件跳过(数组中的值(全局.Cheats, 1), 1);
        追踪全局变量频率(Power, 0, 乘(0.131, 全局.PowerUsage), 速率及最终值);
        //lbl_0:
        大字体信息(所有玩家(队伍1), 自定义字符串("SURVIVE UNTIL 6 AM"));
        设置全局变量(GenjiAI, 地图工坊设置整数(自定义字符串("CUSTOM NIGHT SETTINGS"), 自定义字符串("GENJI A.I LEVEL"), 0, 0, 20, 0));
        设置全局变量(EchoAI, 地图工坊设置整数(自定义字符串("CUSTOM NIGHT SETTINGS"), 自定义字符串("ECHO A.I LEVEL"), 0, 0, 20, 0));
        设置全局变量(OrisaAI, 地图工坊设置整数(自定义字符串("CUSTOM NIGHT SETTINGS"), 自定义字符串("ORISA A.I LEVEL"), 0, 0, 20, 0));
        设置全局变量(BastionAI, 地图工坊设置整数(自定义字符串("CUSTOM NIGHT SETTINGS"), 自定义字符串("BASTION A.I LEVEL"), 0, 0, 20, 0));
        设置全局变量(ZenAI, 地图工坊设置整数(自定义字符串("CUSTOM NIGHT SETTINGS"), 自定义字符串("ZENYATTA A.I LEVEL"), 0, 0, 20, 0));
    }
}

禁用 规则 ("BASTION") {
    事件 {
        持续 - 全局;
    }
}

规则 ("[G] Bastion Setup") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber != 5;
    }
    动作 {
        生成机器人(英雄(堡垒), 队伍2, 0, 矢量(-12.74, 7, -49.92), 左);
        设置全局变量(Bastion, 此栏位的玩家(0, 队伍2));
        开始调整玩家大小(全局.Bastion, 1.3, 假);
        开始修改英雄语音(全局.Bastion, 1.5, 真);
        等待直到 (游戏正在进行中, 99999);
        根据条件跳过(非(首个(全局.Cheats)), 1);
        创建地图文本(所有玩家(所有队伍), 自定义字符串("{0} {1} / {2} A.I", 英雄图标字符串(英雄(堡垒)), 全局.BastionPhase, 全局.BastionAI), 全局.Bastion, 1.2, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 175), 默认可见度);
        //lbl_0:
    }
}

规则 ("[SR] Bastion Jumpscare") {
    事件 {
        子程序;
        BastionJumpscare;
    }
    动作 {
        消除所有地图文本;
        If(非(全局.NoJumpscares));
            设置不可见(全局.Player_, 全部);
            设置全局变量(CurrentCam, 0);
            开始强制设置玩家位置(此栏位的玩家(1, 队伍1), 全局.Bastion, 假);
            等待(0.04, 无视条件);
            设置状态(全局.Bastion, 空, 相移, 3);
            设置状态(此栏位的玩家(1, 队伍1), 空, 相移, 3);
            等待(0.12, 无视条件);
            开始强制设置玩家位置(此栏位的玩家(1, 队伍1), 矢量(0, 200, 0), 假);
            播放效果(所有玩家(所有队伍), 安娜生物手雷禁疗声音, 颜色(队伍1), 全局.Player_, 200);
            播放效果(所有玩家(所有队伍), “黑影”标志声音, 颜色(队伍1), 全局.Player_, 200);
            开始修改英雄语音(全局.Player_, 0.5, 假);
            为玩家添加生命池(全局.Player_, 护盾, 100, 真, 真);
            伤害(全局.Player_, 空, 300);
            传送(全局.Bastion, 所选位置(全局.Player_));
            等待(0.025, 无视条件);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Bastion), 乘(面朝方向(全局.Bastion), 0.7))), 加(逐帧更新(眼睛位置(全局.Bastion)), 全局.CamShake), 0);
            开始规则(CameraShake, 重新开始规则);
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Bastion);
        Else;
            设置全局变量(CurrentCam, 0);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Bastion), 乘(面朝方向(全局.Bastion), 1.5))), 逐帧更新(眼睛位置(全局.Bastion)), 0);
            小字体信息(所有玩家(所有队伍), 自定义字符串("DEAD!"));
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Bastion);
        End;
    }
}

规则 ("[G] Bastion Progression") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        (全局.Bastion).HasMoved == 真;
        全局.BastionAI != 0;
        全局.BastionPhase != -3;
        全局.BastionPhase != 3;
    }
    动作 {
        等待(加(5.5, 除(全局.BastionAI, 6)), 当为“假”时中止);
        If(比较(随机整数(1, 20), <=, 全局.BastionAI));
            设置玩家变量(全局.Bastion, HasMoved, 假);
            If(比较(全局.BastionPhase, ==, 0));
                设置全局变量(BastionPhase, 数组随机取值(数组(1, -1)));
            Else;
                修改全局变量(BastionPhase, 加, If-Then-Else(比较(全局.BastionPhase, >, 0), 1, -1));
            End;
        End;
        循环;
    }
}

规则 ("[G] Bastion Phase 0") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.BastionAI != 0;
        全局.BastionPhase == 0;
    }
    动作 {
        等待直到 (比较(全局.CurrentCam, !=, 1), 99999);
        根据条件中止(比较(全局.BastionPhase, !=, 0));
        传送(全局.Bastion, 矢量(-12.74, 7, -49.92));
        设置朝向(全局.Bastion, 左, 至地图);
        设置玩家变量(全局.Bastion, HasMoved, 真);
    }
}

规则 ("[G] Bastion Phase 1") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.BastionAI != 0;
        全局.BastionPhase == 1;
    }
    动作 {
        等待直到 (比较(全局.CurrentCam, !=, 1), 99999);
        根据条件中止(比较(全局.BastionPhase, !=, 1));
        传送(全局.Bastion, 矢量(6.46, 8.1, -50.77));
        设置朝向(全局.Bastion, 矢量(0.07, 0, 1), 至地图);
        设置玩家变量(全局.Bastion, HasMoved, 真);
    }
}

规则 ("[G] Bastion Phase 2") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.BastionAI != 0;
        全局.BastionPhase == 2;
    }
    动作 {
        等待直到 (与(比较(全局.CurrentCam, !=, 1), 非(与(在视线内(眼睛位置(全局.Player_), 矢量(6.08, 8.1, -40.48), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(6.08, 8.1, -40.48), 75)))), 99999);
        根据条件中止(比较(全局.BastionPhase, !=, 2));
        传送(全局.Bastion, 矢量(6.59, 8.01, -45.94));
        设置朝向(全局.Bastion, 矢量(-0.6, 0, 0.8), 至地图);
        设置玩家变量(全局.Bastion, HasMoved, 真);
    }
}

规则 ("[G] Bastion Phase 3") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.BastionAI != 0;
        全局.BastionPhase == 3;
    }
    动作 {
        等待直到 (非(与(在视线内(眼睛位置(全局.Player_), 矢量(1.93, 8.43, -30.57), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(1.93, 8.43, -30.57), 75))), 99999);
        根据条件中止(比较(全局.BastionPhase, !=, 3));
        传送(全局.Bastion, 矢量(3.39, 7.06, -31.06));
        设置朝向(全局.Bastion, 矢量(-0.71, 0, 0.71), 至地图);
        等待(减(6, 除(全局.BastionAI, 10)), 无视条件);
        等待直到 (非(全局.CurrentCam), 8);
        If(全局.ClosedLeftDoor);
            等待直到 (与(比较(全局.CurrentCam, !=, 1), 非(与(在视线内(眼睛位置(全局.Player_), 眼睛位置(全局.Bastion), 屏障不会阻挡视线), 在视野内(全局.Player_, 眼睛位置(全局.Bastion), 75)))), 99999);
            If(全局.ClosedLeftDoor);
                设置全局变量(BastionPhase, If-Then-Else(比较(随机整数(1, 100), <=, 全局.BastionAI), 数组随机取值(数组(-1, 1)), 0));
            Else;
                调用子程序(BastionJumpscare);
            End;
        Else;
            调用子程序(BastionJumpscare);
        End;
        设置玩家变量(全局.Bastion, HasMoved, 真);
    }
}

规则 ("[G] Bastion Phase -1") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.BastionAI != 0;
        全局.BastionPhase == -1;
    }
    动作 {
        等待直到 (比较(全局.CurrentCam, !=, 1), 99999);
        根据条件中止(比较(全局.BastionPhase, !=, -1));
        传送(全局.Bastion, 矢量(-10.88, 7, -47));
        设置朝向(全局.Bastion, 矢量(0.02, 0, 1), 至地图);
        设置玩家变量(全局.Bastion, HasMoved, 真);
    }
}

规则 ("[G] Bastion Phase -2") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.BastionAI != 0;
        全局.BastionPhase == -2;
    }
    动作 {
        等待直到 (与(比较(全局.CurrentCam, !=, 1), 比较(全局.CurrentCam, !=, 2)), 99999);
        根据条件中止(比较(全局.BastionPhase, !=, -2));
        传送(全局.Bastion, 矢量(-8.8, 7.34, -33.58));
        设置朝向(全局.Bastion, 矢量(0.02, 0, 1), 至地图);
        设置玩家变量(全局.Bastion, HasMoved, 真);
    }
}

规则 ("[G] Bastion Phase -3") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.BastionAI != 0;
        全局.BastionPhase == -3;
    }
    动作 {
        等待直到 (与(与(比较(全局.CurrentCam, !=, 2), 全局.OrisaMeter), 非(与(在视线内(眼睛位置(全局.Player_), 矢量(-4.38, 9, -23.22), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(-4.38, 9, -23.22), 75)))), 99999);
        根据条件中止(比较(全局.BastionPhase, !=, -3));
        传送(全局.Bastion, 矢量(-4.38, 7.04, -23.22));
        设置朝向(全局.Bastion, 矢量(0.71, 0, -0.71), 至地图);
        等待(减(6, 除(全局.BastionAI, 10)), 无视条件);
        等待直到 (非(全局.CurrentCam), 8);
        If(全局.ClosedRightDoor);
            等待直到 (与(比较(全局.CurrentCam, !=, 1), 非(与(在视线内(眼睛位置(全局.Player_), 眼睛位置(全局.Bastion), 屏障不会阻挡视线), 在视野内(全局.Player_, 眼睛位置(全局.Bastion), 75)))), 99999);
            If(全局.ClosedRightDoor);
                设置全局变量(BastionPhase, If-Then-Else(比较(随机整数(1, 100), <=, 全局.BastionAI), 数组随机取值(数组(-1, 1)), 0));
            Else;
                调用子程序(BastionJumpscare);
            End;
        Else;
            调用子程序(BastionJumpscare);
        End;
        设置玩家变量(全局.Bastion, HasMoved, 真);
    }
}

禁用 规则 ("ECHO") {
    事件 {
        持续 - 全局;
    }
}

规则 ("[G] Echo Setup") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber != 5;
    }
    动作 {
        等待(0.25, 无视条件);
        生成机器人(英雄(回声), 队伍2, 2, 矢量(28.62, 3, 2.9), 前);
        设置全局变量(Echo, 此栏位的玩家(2, 队伍2));
        开始调整玩家大小(全局.Echo, 1.2, 假);
        等待直到 (游戏正在进行中, 99999);
        设置状态(选择英雄的玩家(英雄(回声), 所有队伍), 空, 击倒, 9999);
        根据条件跳过(非(首个(全局.Cheats)), 1);
        创建地图文本(所有玩家(所有队伍), 自定义字符串("{0} {1} / {2} A.I", 英雄图标字符串(英雄(回声)), 全局.EchoPhase, 全局.EchoAI), 全局.Echo, 1.2, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 175), 默认可见度);
        //lbl_0:
    }
}

规则 ("[SR] Echo Jumpscare") {
    事件 {
        子程序;
        EchoJumpscare;
    }
    动作 {
        消除所有地图文本;
        If(非(全局.NoJumpscares));
            设置不可见(全局.Player_, 全部);
            设置全局变量(CurrentCam, 0);
            开始强制设置玩家位置(此栏位的玩家(1, 队伍1), 全局.Echo, 假);
            等待(0.04, 无视条件);
            设置状态(全局.Echo, 空, 相移, 3);
            设置状态(此栏位的玩家(1, 队伍1), 空, 相移, 3);
            等待(0.12, 无视条件);
            开始强制设置玩家位置(此栏位的玩家(1, 队伍1), 矢量(0, 200, 0), 假);
            播放效果(所有玩家(所有队伍), 安娜生物手雷禁疗声音, 颜色(队伍1), 全局.Player_, 200);
            播放效果(所有玩家(所有队伍), “黑影”标志声音, 颜色(队伍1), 全局.Player_, 200);
            开始修改英雄语音(全局.Player_, 0.5, 假);
            为玩家添加生命池(全局.Player_, 护盾, 100, 真, 真);
            伤害(全局.Player_, 空, 300);
            传送(全局.Echo, 所选位置(全局.Player_));
            等待(0.016, 无视条件);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Echo), 乘(面朝方向(全局.Echo), 0.6))), 加(逐帧更新(眼睛位置(全局.Echo)), 全局.CamShake), 0);
            按下按键(全局.Echo, 按钮(技能2));
            开始规则(CameraShake, 重新开始规则);
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Echo);
        Else;
            设置全局变量(CurrentCam, 0);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Echo), 乘(面朝方向(全局.Echo), 1.5))), 逐帧更新(眼睛位置(全局.Echo)), 0);
            小字体信息(所有玩家(所有队伍), 自定义字符串("DEAD!"));
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Echo);
        End;
    }
}

规则 ("[G] Echo Progression") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        (全局.Echo).HasMoved == 真;
        全局.EchoAI != 0;
        全局.EchoPhase != 4;
        全局.EchoPhase != -4;
    }
    动作 {
        等待(加(5, 除(全局.EchoAI, 6)), 当为“假”时中止);
        If(比较(随机整数(1, 20), <=, 全局.EchoAI));
            设置玩家变量(全局.Echo, HasMoved, 假);
            If(比较(全局.EchoPhase, ==, 0));
                清除状态(全局.Echo, 击倒);
                设置全局变量(EchoPhase, 数组随机取值(数组(1, -1)));
            Else;
                修改全局变量(EchoPhase, 加, If-Then-Else(比较(全局.EchoPhase, >, 0), 1, -1));
            End;
        End;
        循环;
    }
}

规则 ("[G] Echo Phase 0") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.EchoAI != 0;
        全局.EchoPhase == 0;
    }
    动作 {
        等待直到 (比较(全局.CurrentCam, !=, 4), 99999);
        根据条件中止(比较(全局.EchoPhase, !=, 0));
        传送(全局.Echo, 矢量(28.62, 3, 2.9));
        设置朝向(全局.Echo, 前, 至地图);
        设置玩家变量(全局.Echo, HasMoved, 真);
    }
}

规则 ("[G] Echo Phase 1") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.EchoAI != 0;
        全局.EchoPhase == 1;
    }
    动作 {
        等待直到 (比较(全局.CurrentCam, !=, 4), 99999);
        根据条件中止(比较(全局.EchoPhase, !=, 1));
        传送(全局.Echo, 矢量(22.4, 3, 2.57));
        设置朝向(全局.Echo, 右, 至地图);
        设置玩家变量(全局.Echo, HasMoved, 真);
    }
}

规则 ("[G] Echo Phase 2") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.EchoAI != 0;
        全局.EchoPhase == 2;
    }
    动作 {
        等待直到 (与(比较(全局.CurrentCam, !=, 4), 比较(全局.CurrentCam, !=, 5)), 99999);
        根据条件中止(比较(全局.EchoPhase, !=, 2));
        传送(全局.Echo, 矢量(18.33, 4, -6.3));
        设置朝向(全局.Echo, 矢量(-0.22, 0.25, -0.94), 至地图);
        设置玩家变量(全局.Echo, HasMoved, 真);
    }
}

规则 ("[G] Echo Phase 3") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.EchoAI != 0;
        全局.EchoPhase == 3;
    }
    动作 {
        等待直到 (与(比较(全局.CurrentCam, !=, 5), 非(与(在视线内(眼睛位置(全局.Player_), 矢量(16.39, 7.02, -14.72), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(16.39, 7.02, -14.72), 75)))), 99999);
        根据条件中止(比较(全局.EchoPhase, !=, 3));
        传送(全局.Echo, 矢量(17.18, 6.02, -13.96));
        设置朝向(全局.Echo, 矢量(-0.86, 0.1, -0.5), 至地图);
        设置玩家变量(全局.Echo, HasMoved, 真);
    }
}

规则 ("[G] Echo Phase -1") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.EchoAI != 0;
        全局.EchoPhase == -1;
    }
    动作 {
        等待直到 (与(比较(全局.CurrentCam, !=, 4), 比较(全局.CurrentCam, !=, 6)), 99999);
        根据条件中止(比较(全局.EchoPhase, !=, -1));
        传送(全局.Echo, 矢量(27.3, 8.96, 2.53));
        设置朝向(全局.Echo, 矢量(-0.45, 0, -0.89), 至地图);
        设置玩家变量(全局.Echo, HasMoved, 真);
    }
}

规则 ("[G] Echo Phase -2") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.EchoAI != 0;
        全局.EchoPhase == -2;
    }
    动作 {
        等待直到 (比较(全局.CurrentCam, !=, 6), 99999);
        根据条件中止(比较(全局.EchoPhase, !=, -2));
        传送(全局.Echo, 矢量(22.8, 7.25, -11.8));
        设置朝向(全局.Echo, 方向(眼睛位置(全局.Echo), 数组中的值(全局.CamPositions, 6)), 至地图);
        设置玩家变量(全局.Echo, HasMoved, 真);
    }
}

规则 ("[G] Echo Phase -3") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.EchoAI != 0;
        全局.EchoPhase == -3;
    }
    动作 {
        等待直到 (与(比较(全局.CurrentCam, !=, 6), 非(与(在视线内(眼睛位置(全局.Player_), 矢量(14.55, 13.1, -20.3), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(14.55, 13.1, -20.3), 75)))), 99999);
        根据条件中止(比较(全局.EchoPhase, !=, -3));
        传送(全局.Echo, 矢量(15.5, 11.81, -21));
        设置朝向(全局.Echo, 矢量(-0.9, -0.5, -0.05), 至地图);
        设置玩家变量(全局.Echo, HasMoved, 真);
    }
}

规则 ("[G] Echo Phase 4 and -4") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.EchoAI != 0;
        或(比较(全局.EchoPhase, ==, 4), 比较(全局.EchoPhase, ==, -4)) == 真;
    }
    动作 {
        等待直到 (与(与(比较(全局.CurrentCam, !=, 4), 比较(全局.CurrentCam, !=, 6)), 非(与(在视线内(眼睛位置(全局.Player_), 矢量(6.74, 9.02, -19.94), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(6.74, 9.02, -19.94), 75)))), 99999);
        根据条件中止(与(比较(全局.EchoPhase, !=, 4), 比较(全局.EchoPhase, !=, -4)));
        传送(全局.Echo, 矢量(6.74, 7.02, -19.94));
        设置朝向(全局.Echo, 矢量(-0.71, 0.1, -0.71), 至地图);
        等待(减(6, 除(全局.EchoAI, 10)), 无视条件);
        等待直到 (非(全局.CurrentCam), 5);
        If(全局.ClosedFrontDoor);
            等待直到 (非(与(在视线内(眼睛位置(全局.Player_), 眼睛位置(全局.Echo), 屏障不会阻挡视线), 在视野内(全局.Player_, 眼睛位置(全局.Echo), 75))), 99999);
            If(非(在视线内(眼睛位置(全局.Player_), 眼睛位置(全局.Echo), 屏障不会阻挡视线)));
                等待(随机实数(1, 3), 无视条件);
                根据条件循环(在视线内(眼睛位置(全局.Player_), 眼睛位置(全局.Echo), 屏障不会阻挡视线));
            Else If(非(在视野内(全局.Player_, 眼睛位置(全局.Echo), 75)));
                开始规则(CamsJammed, 重新开始规则);
                设置全局变量(Power, 较大(0, 减(全局.Power, 3)));
            End;
            设置全局变量(EchoPhase, If-Then-Else(比较(随机整数(1, 100), <=, 全局.EchoAI), 数组随机取值(数组(-1, 1)), 0));
        Else;
            调用子程序(EchoJumpscare);
        End;
        设置玩家变量(全局.Echo, HasMoved, 真);
    }
}

禁用 规则 ("ORISA") {
    事件 {
        持续 - 全局;
    }
}

规则 ("[G] Orisa Setup") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber != 5;
    }
    动作 {
        等待(0.5, 无视条件);
        生成机器人(英雄(奥丽莎), 队伍2, 1, 矢量(-10.7, 8.52, -0.37), 右);
        设置全局变量(Orisa, 此栏位的玩家(1, 队伍2));
        开始调整玩家大小(全局.Orisa, 1.35, 假);
        等待直到 (游戏正在进行中, 99999);
        设置状态(选择英雄的玩家(英雄(奥丽莎), 所有队伍), 空, 击倒, 9999);
        设置全局变量(OrisaMeter, 100);
        根据条件跳过(非(首个(全局.Cheats)), 1);
        创建地图文本(所有玩家(所有队伍), 自定义字符串("{0} {1} / {2} A.I", 英雄图标字符串(英雄(奥丽莎)), 全局.OrisaMeter, 全局.OrisaAI), 全局.Orisa, 1.2, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 175), 默认可见度);
        //lbl_0:
    }
}

规则 ("[SR] Orisa Jumpscare") {
    事件 {
        子程序;
        OrisaJumpscare;
    }
    动作 {
        消除所有地图文本;
        If(非(全局.NoJumpscares));
            设置不可见(全局.Player_, 全部);
            设置全局变量(CurrentCam, 0);
            播放效果(所有玩家(所有队伍), 安娜生物手雷禁疗声音, 颜色(队伍1), 全局.Player_, 200);
            播放效果(所有玩家(所有队伍), “黑影”标志声音, 颜色(队伍1), 全局.Player_, 200);
            开始修改英雄语音(全局.Player_, 0.5, 假);
            为玩家添加生命池(全局.Player_, 护盾, 100, 真, 真);
            伤害(全局.Player_, 空, 300);
            等待(0.016, 无视条件);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Orisa), 乘(面朝方向(全局.Orisa), 1.2))), 加(逐帧更新(眼睛位置(全局.Orisa)), 全局.CamShake), 0);
            伤害(全局.Orisa, 空, 390);
            开始规则(CameraShake, 重新开始规则);
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Orisa);
        Else;
            设置全局变量(CurrentCam, 0);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Orisa), 乘(面朝方向(全局.Orisa), 1.5))), 逐帧更新(眼睛位置(全局.Orisa)), 0);
            小字体信息(所有玩家(所有队伍), 自定义字符串("DEAD!"));
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Orisa);
        End;
    }
}

规则 ("[G] Orisa Progression") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.OrisaAI != 0;
    }
    动作 {
        追踪全局变量频率(OrisaMeter, 0, 加(0.45, 除(全局.OrisaAI, 15)), 速率及最终值);
        等待直到 (与(比较(全局.CurrentCam, !=, 3), 比较(全局.OrisaMeter, <=, 66)), 99999);
        清除状态(全局.Orisa, 击倒);
        设置朝向(全局.Orisa, 方向(眼睛位置(全局.Orisa), 数组中的值(全局.CamPositions, 3)), 至地图);
        等待直到 (与(比较(全局.CurrentCam, !=, 3), 比较(全局.OrisaMeter, <=, 23)), 99999);
        传送(全局.Orisa, 矢量(-10.36, 7, -8.25));
        设置朝向(全局.Orisa, 右, 至地图);
        等待直到 (与(与(与(比较(全局.CurrentCam, !=, 3), 比较(全局.OrisaMeter, <=, 0)), 比较(全局.BastionPhase, !=, 3)), 非(与(在视线内(眼睛位置(全局.Player_), 矢量(-15.57, 7.42, -11.65), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(-15.57, 7.42, -11.65), 75)))), 99999);
        传送(全局.Orisa, 矢量(-15.57, 6.42, -11.65));
        设置朝向(全局.Orisa, 矢量(0.68, 0, -0.73), 至地图);
        等待直到 (非(全局.CurrentCam), 5);
        开始定向阈值(全局.Orisa, 方向(所选位置(全局.Orisa), 矢量(-4.2, 7.07, -23.51)), 1, 至地图, 替换现有阈值, 方向和幅度);
        等待直到 (比较(相距距离(所选位置(全局.Orisa), 矢量(-4.2, 7.07, -23.51)), <=, 0.3), 99999);
        If(全局.ClosedRightDoor);
            设置状态(全局.Orisa, 全局.Player_, 击晕, 1);
            停止定向阈值(全局.Orisa);
            等待直到 (与(比较(全局.CurrentCam, !=, 3), 非(与(在视线内(眼睛位置(全局.Player_), 眼睛位置(全局.Orisa), 屏障不会阻挡视线), 在视野内(全局.Player_, 眼睛位置(全局.Orisa), 75)))), 99999);
            If(全局.ClosedRightDoor);
                传送(全局.Orisa, 矢量(-10.7, 8.52, -0.37));
                设置朝向(全局.Orisa, 方向(眼睛位置(全局.Orisa), 数组中的值(全局.CamPositions, 3)), 至地图);
                设置全局变量(OrisaMeter, 100);
                如条件为“真”则循环;
            Else;
                调用子程序(OrisaJumpscare);
            End;
        Else;
            调用子程序(OrisaJumpscare);
    }
}

规则 ("[G] Update Orisa Speed") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.OrisaAI != 0;
        全局.OrisaAIPrevious != 全局.OrisaAI;
    }
    动作 {
        设置全局变量(OrisaAIPrevious, 全局.OrisaAI);
        设置移动速度(全局.Orisa, 加(165, 乘(全局.OrisaAI, 1.5)));
    }
}

规则 ("[G] Orisa Stall Start") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.CurrentCam == 3;
        全局.OrisaAI != 0;
        全局.OrisaMeter > 0;
    }
    动作 {
        停止追踪全局变量(OrisaMeter);
        等待(随机实数(1.4, 2.2), 当为“假”时中止);
        设置全局变量(OrisaMeter, 较小(100, 加(全局.OrisaMeter, 0.9)));
        如条件为“真”则循环;
    }
}

规则 ("[G] Orisa Stall Stop") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.CurrentCam != 3;
        全局.OrisaAI != 0;
    }
    动作 {
        等待(0.25, 当为“假”时中止);
        追踪全局变量频率(OrisaMeter, 0, 加(0.45, 除(全局.OrisaAI, 15)), 速率及最终值);
    }
}

禁用 规则 ("GENJI") {
    事件 {
        持续 - 全局;
    }
}

规则 ("[G] Genji Setup") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber != 5;
        全局.NightNumber > 1;
    }
    动作 {
        等待(0.75, 无视条件);
        生成机器人(英雄(源氏), 队伍2, 3, 矢量(-2.6, 7, 54.5), 矢量(-0.7, 0, -0.71));
        设置全局变量(Genji, 此栏位的玩家(3, 队伍2));
        开始调整玩家大小(全局.Genji, 1.6, 假);
        等待直到 (游戏正在进行中, 99999);
        设置状态(选择英雄的玩家(英雄(源氏), 所有队伍), 空, 击倒, 9999);
        设置全局变量(HeaterPower, 100);
        根据条件跳过(非(首个(全局.Cheats)), 1);
        创建地图文本(所有玩家(所有队伍), 自定义字符串("{0} {1} / {2} A.I", 英雄图标字符串(英雄(源氏)), 全局.HeaterPower, 全局.GenjiAI), 全局.Genji, 1.2, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 175), 默认可见度);
        //lbl_0:
        创建效果(所有玩家(所有队伍), 查莉娅粒子光束声音, 颜色(队伍2), 数组中的值(全局.CamPositions, 7), 除(全局.HeaterPower, 2), 可见，位置和半径);
        创建地图文本(If-Then-Else(比较(全局.CurrentCam, ==, 7), 全局.Player_, 空), 截取字符串(自定义字符串("■■■■■■■■■■■■■■■■"), 0, 除(全局.HeaterPower, 6.25)), 逐帧更新(加(乘(与此角度的相对方向(水平朝向角度(全局.Player_), 加(垂直朝向角度(全局.Player_), 22)), 1000), 数组中的值(全局.CamPositions, 7))), 2.7, 不要截取, 可见，位置，字符串和颜色, 自定义颜色(255, 乘(全局.HeaterPower, 2.55), 全局.HeaterPower, If-Then-Else(比较(除(全局.HeaterPower, 6.25), >, 15.5), 215, 160)), 默认可见度);
        创建地图文本(If-Then-Else(比较(全局.CurrentCam, ==, 7), 本地玩家, 空), 自定义字符串("HOLD [{0}] TO WIND UP HEATER", 输入绑定字符串(按钮(装填))), 逐帧更新(加(乘(与此角度的相对方向(水平朝向角度(本地玩家), 加(垂直朝向角度(本地玩家), 24.5)), 1000), 数组中的值(全局.CamPositions, 7))), 1.7, 不要截取, 可见，位置，字符串和颜色, 自定义颜色(255, 255, 255, If-Then-Else(按钮被按下(本地玩家, 按钮(装填)), 255, 140)), 默认可见度);
        创建图标(If-Then-Else(全局.CurrentCam, 空, 全局.WarningIconVisible), 全局.InteractCams, 火焰, 可见和位置, 自定义颜色(255, 80, 0, 255), 真);
        创建HUD文本(If-Then-Else(全局.CurrentCam, 全局.WarningIconVisible, 空), 空, 空, 自定义字符串("CAM 7"), 顶部, 2, 空, 空, 自定义颜色(255, 80, 0, 255), 可见, 默认可见度);
    }
}

规则 ("[SR] Genji Jumpscare") {
    事件 {
        子程序;
        GenjiJumpscare;
    }
    动作 {
        消除所有地图文本;
        If(非(全局.NoJumpscares));
            传送(全局.Genji, 射线命中位置(眼睛位置(全局.Player_), 加(眼睛位置(全局.Player_), 乘(面朝方向(全局.Player_), 矢量(-3, 0, -3))), 空, 全局.Player_, 假));
            开始朝向(全局.Genji, 方向(眼睛位置(全局.Genji), 眼睛位置(全局.Player_)), 360, 至地图, 方向及角速率);
            等待直到 (非(全局.CurrentCam), 随机实数(4, 8));
            等待直到 (在视野内(全局.Player_, 眼睛位置(全局.Genji), 50), 随机实数(4, 8));
            等待(0.5, 无视条件);
            播放效果(所有玩家(所有队伍), 安娜生物手雷禁疗声音, 颜色(队伍1), 全局.Player_, 200);
            播放效果(所有玩家(所有队伍), “黑影”标志声音, 颜色(队伍1), 全局.Player_, 200);
            开始修改英雄语音(全局.Player_, 0.5, 假);
            为玩家添加生命池(全局.Player_, 护盾, 100, 真, 真);
            伤害(全局.Player_, 空, 300);
            等待(0.016, 无视条件);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Genji), 乘(面朝方向(全局.Genji), 1.2))), 加(逐帧更新(眼睛位置(全局.Genji)), 全局.CamShake), 0);
            开始规则(CameraShake, 重新开始规则);
            按下按键(全局.Genji, 按钮(终极技能));
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Genji);
        Else;
            传送(全局.Genji, 射线命中位置(眼睛位置(全局.Player_), 加(眼睛位置(全局.Player_), 乘(面朝方向(全局.Player_), 矢量(-3, 0, -3))), 空, 全局.Player_, 假));
            开始朝向(全局.Genji, 方向(眼睛位置(全局.Genji), 眼睛位置(全局.Player_)), 360, 至地图, 方向及角速率);
            等待直到 (非(全局.CurrentCam), 随机实数(4, 8));
            等待直到 (在视野内(全局.Player_, 眼睛位置(全局.Genji), 50), 随机实数(4, 8));
            等待(0.5, 无视条件);
            设置全局变量(CurrentCam, 0);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Genji), 乘(面朝方向(全局.Genji), 1.5))), 逐帧更新(眼睛位置(全局.Genji)), 0);
            小字体信息(所有玩家(所有队伍), 自定义字符串("DEAD!"));
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Genji);
        End;
    }
}

规则 ("[G] Genji Progression") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.GenjiAI != 0;
        全局.HeaterPower <= 0;
    }
    动作 {
        等待直到 (与(比较(全局.CurrentCam, !=, 7), 比较(全局.HeaterPower, <=, 0)), 99999);
        清除状态(全局.Genji, 击倒);
        设置朝向(全局.Genji, 方向(眼睛位置(全局.Genji), 数组中的值(全局.CamPositions, 7)), 至地图);
        等待(随机实数(2.2, 减(4.4, 除(全局.GenjiAI, 10))), 无视条件);
        等待直到 (与(比较(全局.CurrentCam, !=, 7), 比较(全局.HeaterPower, <=, 0)), 99999);
        传送(全局.Genji, 矢量(-12.56, 7, 45.08));
        设置朝向(全局.Genji, 矢量(0.71, 0, -0.71), 至地图);
        等待(随机实数(1.8, 减(4, 除(全局.GenjiAI, 10))), 无视条件);
        等待直到 (与(比较(全局.CurrentCam, !=, 7), 比较(全局.HeaterPower, <=, 0)), 99999);
        调用子程序(GenjiJumpscare);
    }
}

规则 ("[G] Start Wind Down") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.GenjiAI != 0;
        游戏正在进行中 == 真;
    }
    动作 {
        追踪全局变量频率(HeaterPower, 0, 加(2.3, 除(全局.GenjiAI, 6.2)), 速率及最终值);
    }
}

规则 ("[G] Wind Up") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.CurrentCam == 7;
        按钮被按下(全局.Player_, 按钮(装填)) == 真;
        全局.GenjiAI != 0;
    }
    动作 {
        播放效果(所有玩家(所有队伍), 布丽吉塔流星飞锤范围治疗声音, 颜色(队伍2), 数组中的值(全局.CamPositions, 7), 25);
        追踪全局变量频率(HeaterPower, 100, 35, 全部禁用);
        等待直到 (或(非(按钮被按下(全局.Player_, 按钮(装填))), 比较(全局.CurrentCam, !=, 7)), 99999);
        追踪全局变量频率(HeaterPower, 0, 加(2.3, 除(全局.GenjiAI, 6.2)), 速率及最终值);
    }
}

规则 ("[G] Warning") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.GenjiAI != 0;
        全局.HeaterPower < 28;
    }
    动作 {
        设置全局变量(WarningIconVisible, 所有玩家(所有队伍));
        等待(0.2, 无视条件);
        设置全局变量(WarningIconVisible, 空);
        等待(0.2, 无视条件);
        如条件为“真”则循环;
    }
}

禁用 规则 ("ZEN") {
    事件 {
        持续 - 全局;
    }
}

规则 ("[G] Zen Setup") {
    事件 {
        持续 - 全局;
    }
    条件 {
        全局.NightNumber >= 1;
    }
    动作 {
        等待(1, 无视条件);
        生成机器人(英雄(禅雅塔), 队伍2, 4, 矢量(2, 7.4, 22.4), 左);
        设置全局变量(Zen, 此栏位的玩家(4, 队伍2));
        开始调整玩家大小(全局.Zen, 1.35, 假);
        设置全局变量(ZenPositions, 数组(矢量(-1.92, 7, -54.26), 矢量(-9.09, 7.23, -30.7), 矢量(-10.11, 8.51, -1.69), 矢量(28.2, 3, 1.2), 矢量(18, 4.9, -9.52), 矢量(23.9, 9, -5.63), 矢量(-4.23, 7, 54.36)));
        设置全局变量(ZenPositions2, 数组(空, 数组(矢量(-8.69, 7, -49.87), 矢量(16.82, 8.96, -48.44)), 数组(矢量(-12.25, 7.24, -33.65), 矢量(-2.2, 7.34, -31.7)), 数组(矢量(-20.72, 7, 6.54), 矢量(-19.22, 12, 8.7), 矢量(-10.91, 7, 8.71)), 数组(矢量(23.93, 3.15, -5.11), 矢量(29.37, 3.15, 3.7)), 数组(矢量(18, 5.96, -11.64)), 数组(矢量(23.62, 9, 5.63), 矢量(23.1, 6.86, -12.56)), 数组(矢量(3.34, 8, 53.94), 矢量(-13.06, 7, 44.26))));
        设置不可见(全局.Zen, 全部);
        设置受到治疗(全局.Zen, 0);
        取消与玩家的移动碰撞(全局.Zen);
        开始朝向(全局.Zen, 方向(眼睛位置(全局.Zen), 眼睛位置(全局.Player_)), 1000, 至地图, 方向及角速率);
        关闭游戏预设复生模式(全局.Zen);
    }
}

规则 ("[SR] Zen Jumpscare Nights 1-5") {
    事件 {
        子程序;
        ZenSequence;
    }
    动作 {
        设置不可见(全局.Zen, 无);
        开始朝向(全局.Zen, 方向(眼睛位置(全局.Zen), 数组中的值(全局.CamPositions, 全局.CurrentCam)), 1000, 至地图, 方向及角速率);
        传送(全局.Zen, 首个(已排序的数组(全局.ZenPositions, 相距距离(当前数组元素, 数组中的值(全局.CamPositions, 全局.CurrentCam)))));
        等待直到 (或(或(按钮被按下(全局.Player_, 按钮(主要攻击模式)), 按钮被按下(全局.Player_, 按钮(辅助攻击模式))), 按钮被按下(全局.Player_, 按钮(互动))), 减(1.5, 除(全局.ZenAI, 30)));
        If(或(或(按钮被按下(全局.Player_, 按钮(主要攻击模式)), 按钮被按下(全局.Player_, 按钮(辅助攻击模式))), 按钮被按下(全局.Player_, 按钮(互动))));
            设置不可见(全局.Zen, 全部);
            等待(4, 无视条件);
            中止;
        Else;
            等待直到 (比较(全局.CurrentCam, !=, 全局.LastCam), 9999);
            传送(全局.Zen, 矢量(3.9, 7.4, -25.5));
            等待直到 (非(全局.CurrentCam), 9999);
            等待(0.1, 无视条件);
            开始朝向(全局.Zen, 方向(眼睛位置(全局.Zen), 眼睛位置(全局.Player_)), 1000, 至地图, 方向及角速率);
            设置朝向(全局.Player_, 方向(眼睛位置(全局.Player_), 眼睛位置(全局.Zen)), 至地图);
            等待直到 (全局.CurrentCam, 减(1.7, 除(全局.ZenAI, 30)));
            If(全局.CurrentCam);
                设置不可见(全局.Zen, 全部);
                传送(全局.Zen, 首个(已排序的数组(全局.ZenPositions, 相距距离(当前数组元素, 数组中的值(全局.CamPositions, 全局.CurrentCam)))));
                等待(4, 无视条件);
                中止;
            Else;
                消除所有地图文本;
                If(非(全局.NoJumpscares));
                    播放效果(所有玩家(所有队伍), 安娜生物手雷禁疗声音, 颜色(队伍1), 全局.Player_, 200);
                    播放效果(所有玩家(所有队伍), “黑影”标志声音, 颜色(队伍1), 全局.Player_, 200);
                    开始修改英雄语音(全局.Player_, 0.5, 假);
                    为玩家添加生命池(全局.Player_, 护盾, 100, 真, 真);
                    伤害(全局.Player_, 空, 300);
                    等待(0.016, 无视条件);
                    开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Zen), 面朝方向(全局.Zen))), 加(逐帧更新(眼睛位置(全局.Zen)), 全局.CamShake), 0);
                    开始规则(CameraShake, 重新开始规则);
                    按下按键(全局.Zen, 按钮(终极技能));
                    等待(1.5, 无视条件);
                    击杀(全局.Player_, 全局.Zen);
                Else;
                    设置全局变量(CurrentCam, 0);
                    开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Zen), 面朝方向(全局.Zen))), 逐帧更新(眼睛位置(全局.Zen)), 0);
                    小字体信息(所有玩家(所有队伍), 自定义字符串("DEAD!"));
                    等待(1.5, 无视条件);
                    击杀(全局.Player_, 全局.Zen);
                End;
            End;
        End;
    }
}

禁用 规则 ("NIGHT 6") {
    事件 {
        持续 - 全局;
    }
}

规则 ("[G] Zen Progression") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.NightNumber == 5;
        全局.NightPhase == 6;
        存活(全局.Zen) == 真;
        全局.ZenDoorNumber == 0;
        全局.CurrentCam != 首个(全局.ZenPickedCamAndPos);
        全局.ZenAI > 0;
    }
    动作 {
        开始朝向(全局.Zen, 方向(眼睛位置(全局.Zen), 首个(已排序的数组(从数组中移除(全局.CamPositions, 空), 相距距离(眼睛位置(全局.Zen), 当前数组元素)))), 1000, 至地图, 方向及角速率);
        等待(减(6.5, 除(全局.ZenAI, 15)), 当为“假”时中止);
        If(比较(随机整数(1, 20), >, 全局.ZenAI));
            调用子程序(ZenMove);
        Else;
            开始朝向(全局.Zen, 方向(眼睛位置(全局.Zen), 眼睛位置(全局.Player_)), 1000, 至地图, 方向及角速率);
            调用子程序(ZenDoor);
        End;
        如条件为“真”则循环;
    }
}

规则 ("[SR] Zen Jumpscare Night 6") {
    事件 {
        子程序;
        ZenJumpscare;
    }
    动作 {
        开启与玩家的移动碰撞(全局.Zen);
        设置不可见(全局.Zen, 无);
        消除所有地图文本;
        停止朝向(全局.Zen);
        If(非(全局.NoJumpscares));
            设置不可见(全局.Player_, 全部);
            设置全局变量(CurrentCam, 0);
            开始强制设置玩家位置(此栏位的玩家(1, 队伍1), 全局.Zen, 假);
            等待(0.04, 无视条件);
            设置状态(全局.Zen, 空, 相移, 3);
            设置状态(此栏位的玩家(1, 队伍1), 空, 相移, 3);
            等待(0.12, 无视条件);
            开始强制设置玩家位置(此栏位的玩家(1, 队伍1), 矢量(0, 200, 0), 假);
            播放效果(所有玩家(所有队伍), 安娜生物手雷禁疗声音, 颜色(队伍1), 全局.Player_, 200);
            播放效果(所有玩家(所有队伍), “黑影”标志声音, 颜色(队伍1), 全局.Player_, 200);
            开始修改英雄语音(全局.Player_, 0.5, 假);
            为玩家添加生命池(全局.Player_, 护盾, 100, 真, 真);
            伤害(全局.Player_, 空, 300);
            传送(全局.Zen, 所选位置(全局.Player_));
            等待(0.025, 无视条件);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Zen), 乘(面朝方向(全局.Zen), 0.7))), 加(逐帧更新(眼睛位置(全局.Zen)), 全局.CamShake), 0);
            开始规则(CameraShake, 重新开始规则);
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Zen);
        Else;
            设置全局变量(CurrentCam, 0);
            开始镜头(全局.Player_, 逐帧更新(加(眼睛位置(全局.Zen), 乘(面朝方向(全局.Zen), 1.5))), 逐帧更新(眼睛位置(全局.Zen)), 0);
            小字体信息(所有玩家(所有队伍), 自定义字符串("DEAD!"));
            等待(1.5, 无视条件);
            击杀(全局.Player_, 全局.Zen);
        End;
    }
}

规则 ("[SR] Night 6 Call") {
    事件 {
        子程序;
        Night6Call;
    }
    动作 {
        等待(0.5, 无视条件);
        设置全局变量(PhoneRings, 3);
        While(与(全局.PhoneRings, 非(按钮被按下(全局.Player_, 按钮(技能1)))));
            调用子程序(PhoneRing);
            修改全局变量(PhoneRings, 减, 1);
            等待直到 (按钮被按下(全局.Player_, 按钮(技能1)), 2);
        End;
        根据条件中止(非(按钮被按下(全局.Player_, 按钮(技能1))));
        设置全局变量(PhoneRings, 0);
        等待(0.5, 无视条件);
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 200);
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[?] TESTING... TESTING... 1... 2... 3..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[?] THIS IS VOICE LOG NUMBER NINETEEN..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[?] FROM HEAD OF A.I RESEARCH BOHAI LIAO."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] OUR WORK HAS BEEN SHOWING VERY"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("PROMISING RESULTS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] SPECIALLY WITH THE FRAGMENT THAT OVERWATCH"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("HAS RETRIEVED IN THEIR ANUBIS SIEGE."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] TO HAVE A PIECE OF A GOD PROGRAM IN MY HANDS?"));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] IT IS A PRIVILEGE. AND A RESPONSIBILITY."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] WE CAN ACHIEVE GREATNESS WITH THIS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] I'LL MAKE SURE WE DO."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] I'VE UPLOADED A PROTOTYPE CODE INTO AN OLD SECURITY ROBOT..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] WHICH MUST BE KEPT IN ECOPOINT NOT TO RAISE ANY SUSPICION."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] AT LEAST IN THE NEAR FUTURE."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] A \"FRIEND\" OF MINE WITH CLOSE RELATIONS"));
        等待(0.5, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("TO THE SHAMBALI..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] STRONGLY ADVISED AGAINST THIS..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] STATING I'M MESSING WITH SOMETHING I DON'T UNDERSTAND."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] AND SOMETHING ABOUT ASCENDED PROGRAMS..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] BEING ABLE TO INTERACT WITH THE REALM OF THE DEAD."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] I'VE NEVER HEARD ANYTHING MORE ABSURD THAN THIS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] THEY WILL NEVER UNDERSTAND."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] BUT IT DOESN'T MATTER."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] MY WORK IS ALMOST COMPLETE."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO] THEY WILL SEE."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[LIAO ENDS THE LOG]"));
        播放效果(所有玩家(所有队伍), “士兵：76”疾跑开始声音, 颜色(队伍1), 眼睛位置(全局.Player_), 130);
        等待(3, 无视条件);
    }
}

规则 ("[G] Night 6 Part 1") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.NightNumber == 5;
    }
    动作 {
        根据条件跳过(数组中的值(全局.Cheats, 1), 1);
        追踪全局变量频率(Power, 0, 乘(0.131, 全局.PowerUsage), 速率及最终值);
        //lbl_0:
        调用子程序(Night6Call);
        设置全局变量(NightPhase, 4);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MEI] MAX SAID HE UNCOVERED PROJECTS FOR A FIFTH ROBOT..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MEI] IF I RETRIEVE THEM I MAY BE ABLE TO FIND A WEAKNESS."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MEI] I SHOULD BE ABLE TO DOWNLOAD THEM FROM THE MONITOR STATION..."));
        等待直到 (比较(全局.CurrentCam, !=, 0), 99999);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MEI] THERE IT IS!"));
        创建效果(If-Then-Else(比较(全局.BarValue, !=, 100), 所有玩家(所有队伍), 空), “回声”复制声音, 颜色(队伍1), If-Then-Else(全局.CurrentCam, 数组中的值(全局.CamPositions, 全局.CurrentCam), 全局.InteractCams), 全局.BarValue, 可见，位置和半径);
        创建进度条HUD文本(If-Then-Else(与(全局.CurrentCam, 或(比较(全局.NightPhase, ==, 4), 比较(全局.NightPhase, ==, 5))), 本地玩家, 空), 全局.BarValue, If-Then-Else(比较(全局.NightPhase, ==, 4), 自定义字符串("SIGNAL LOCATED. PRESS [{0}] TO BEGIN TRANSMISSION.", 输入绑定字符串(按钮(技能2))), 首个(全局.BarTextAndColor)), 顶部, 2, 数组中的值(全局.BarTextAndColor, 1), 颜色(白色), 可见，值和颜色, 始终可见);
        在索引处设置全局变量(BarTextAndColor, 1, 颜色(黄色));
        设置全局变量(BarValue, 100);
        等待直到 (与(全局.CurrentCam, 按钮被按下(全局.Player_, 按钮(技能2))), 99999);
        播放效果(所有玩家(所有队伍), 布丽吉塔恢复包击中声音, 颜色(队伍1), 数组中的值(全局.CamPositions, 全局.CurrentCam), 60);
        设置全局变量(NightPhase, 5);
        设置全局变量(BarValue, 0);
        在索引处设置全局变量(BarTextAndColor, 0, 自定义字符串("FETCHING DATA..."));
        在索引处设置全局变量(BarTextAndColor, 1, 颜色(白色));
        追踪全局变量频率(BarValue, 100, 较大(减(100, 全局.BarValue), 3), 速率及最终值);
        等待直到 (比较(全局.BarValue, >=, 100), 99999);
        在索引处设置全局变量(BarTextAndColor, 0, 自定义字符串("DOWNLOADING SCHEMATICS..."));
        设置全局变量(BarValue, 0);
        追踪全局变量频率(BarValue, 100, 20, 全部禁用);
        等待直到 (比较(全局.BarValue, >=, 100), 99999);
        在索引处设置全局变量(BarTextAndColor, 0, 自定义字符串("UPDATING TRANSMITTERS..."));
        设置全局变量(BarValue, 0);
        追踪全局变量频率(BarValue, 100, 25, 全部禁用);
        等待直到 (比较(全局.BarValue, >=, 100), 99999);
        在索引处设置全局变量(BarTextAndColor, 0, 自定义字符串("COMPLETE"));
        在索引处设置全局变量(BarTextAndColor, 1, 颜色(绿色));
        播放效果(所有玩家(所有队伍), 正面状态施加声音, 颜色(队伍2), If-Then-Else(全局.CurrentCam, 数组中的值(全局.CamPositions, 全局.CurrentCam), 全局.InteractCams), 35);
        等待(2, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MEI] WITH THIS I CAN REMOTELY INDUCE AN OVERLOAD..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MEI] BUT IT DRAINS POWER, SO I NEED TO BE CAREFUL."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MEI] NOW I JUST NEED TO FIND IT..."));
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[MEI] BEFORE IT FINDS ME."));
        等待(3, 无视条件);
        大字体信息(所有玩家(所有队伍), 自定义字符串("SHUT THE FIFTH ROBOT DOWN"));
        持续追踪全局变量(BarValue, 0, 4, 全部禁用);
        设置全局变量(BarValue, 0);
        创建进度条地图文本(If-Then-Else(与(全局.CurrentCam, 比较(全局.NightPhase, ==, 6)), 本地玩家, 空), 全局.BarValue, If-Then-Else(全局.BarValue, 自定义字符串("RECHARGING..."), 自定义字符串("[{0}] TO FIRE OVERLOAD\r\n(MUST BE AIMING DIRECTLY AT TARGET)", 输入绑定字符串(按钮(装填)))), 逐帧更新(加(数组中的值(全局.CamPositions, 全局.CurrentCam), 乘(与此角度的相对方向(水平朝向角度(本地玩家), 加(垂直朝向角度(本地玩家), 15)), 1000))), 1, 不要截取, 颜色(橙色), 颜色(灰色), 可见，位置，值和颜色, 默认可见度);
        设置全局变量(NightPhase, 6);
        修改全局变量(ZenAI, 加, 2);
        调用子程序(ZenMove);
        设置不可见(全局.Zen, 无);
        根据条件跳过(非(首个(全局.Cheats)), 1);
        创建地图文本(所有玩家(所有队伍), 自定义字符串("{0} {1} / {2} A.I", 英雄图标字符串(英雄(禅雅塔)), If-Then-Else(全局.ZenDoorNumber, 自定义字符串("OFFICE GATE"), 自定义字符串("CAM {0}", 首个(全局.ZenPickedCamAndPos))), 全局.ZenAI), 全局.Zen, 1.2, 不要截取, 可见，位置和字符串, 自定义颜色(255, 255, 255, 175), 默认可见度);
        //lbl_1:
        创建地图文本(If-Then-Else(全局.CurrentCam, 全局.Player_, 空), 自定义字符串("⊙"), 逐帧更新(加(数组中的值(全局.CamPositions, 全局.CurrentCam), 乘(与此角度的相对方向(水平朝向角度(全局.Player_), 加(垂直朝向角度(全局.Player_), 3.87)), 1000))), 2.3, 不要截取, 可见，位置和字符串, If-Then-Else(全局.BarValue, 颜色(灰色), 颜色(橙色)), 默认可见度);
    }
}

规则 ("[G] Night 6 Part 2") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        死亡(全局.Zen) == 真;
        全局.NightNumber == 5;
        全局.NightPhase == 6;
    }
    动作 {
        消除所有地图文本;
        设置不可见(全局.Zen, 全部);
        设置全局变量(NightPhase, 7);
        等待(3, 无视条件);
        小字体信息(所有玩家(所有队伍), 自定义字符串("[?] THIS IS NOT OVER YET..."));
        等待(3, 无视条件);
        大字体信息(所有玩家(所有队伍), 自定义字符串("ESCAPE"));
        设置全局变量(DoorsWalkable, 1);
        创建效果(所有玩家(所有队伍), 环, 颜色(白色), 矢量(-7.83, 7.02, -2.91), 1.5, 可见);
        创建图标(所有玩家(所有队伍), 矢量(-7.83, 7.02, -2.91), 旗帜, 可见, 颜色(白色), 真);
        等待直到 (比较(相距距离(所选位置(全局.Player_), 矢量(-7.83, 7.02, -2.91)), <=, 1.5), 20);
        If(比较(相距距离(所选位置(全局.Player_), 矢量(-7.83, 7.02, -2.91)), <=, 1.55));
            设置不可见(全局.Player_, 全部);
            开始镜头(全局.Player_, 单次赋值(加(眼睛位置(全局.Player_), 矢量(0, 2, 0))), 矢量(-7.83, 7.02, -2.91), 0);
            消除所有效果;
            开启游戏预设音乐模式;
            宣告队伍胜利(队伍1);
        Else;
            复活(全局.Zen);
            传送(全局.Zen, 所选位置(全局.Player_));
            调用子程序(ZenJumpscare);
    }
}

规则 ("[G] Fire Overload") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.NightNumber == 5;
        全局.NightPhase == 6;
        全局.CurrentCam != 0;
        全局.BarValue <= 0;
        按钮被按下(全局.Player_, 按钮(装填)) == 真;
    }
    动作 {
        设置全局变量(BarValue, 100);
        设置全局变量(Power, 较大(0, 减(全局.Power, 5)));
        修改全局变量(PowerUsage, 减, 1);
        播放效果(所有玩家(所有队伍), 巴蒂斯特生化榴弹枪爆炸声音, 颜色(队伍1), 数组中的值(全局.CamPositions, 全局.CurrentCam), 50);
        播放效果(所有玩家(所有队伍), “黑影”标志声音, 颜色(队伍1), 数组中的值(全局.CamPositions, 全局.CurrentCam), 50);
        播放效果(所有玩家(所有队伍), “黑影”标志效果, 颜色(队伍2), 射线命中位置(数组中的值(全局.CamPositions, 全局.CurrentCam), 加(数组中的值(全局.CamPositions, 全局.CurrentCam), 乘(面朝方向(全局.Player_), 100)), 所有玩家(所有队伍), 空, 假), 1);
        If(比较(射线命中玩家(数组中的值(全局.CamPositions, 全局.CurrentCam), 加(数组中的值(全局.CamPositions, 全局.CurrentCam), 乘(面朝方向(全局.Player_), 100)), 所有玩家(队伍2), 空, 假), ==, 全局.Zen));
            设置造成伤害(全局.Player_, 100);
            根据条件跳过(比较(全局.ZenAI, >, 2), 1);
            小字体信息(所有玩家(所有队伍), 自定义字符串("[MEI] THAT'S IT! JUST A FEW MORE HITS..."));
            //lbl_0:
            伤害(全局.Zen, 全局.Player_, 41);
            开始规则(CamsJammed, 无动作);
            调用子程序(ZenMove);
            等待(0.2, 无视条件);
            设置全局变量(CamsJammed, 4.8);
            修改全局变量(ZenAI, 加, 2);
            设置造成伤害(全局.Player_, 0);
    }
}

规则 ("[G] Stalling in Cam") {
    事件 {
        持续 - 全局;
    }
    条件 {
        游戏正在进行中 == 真;
        全局.NightNumber == 5;
        全局.NightPhase == 6;
        具有状态(全局.Zen, 击晕) == 假;
        全局.CurrentCam == 首个(全局.ZenPickedCamAndPos);
        矢量间夹角(面朝方向(全局.Player_), 方向(数组中的值(全局.CamPositions, 全局.CurrentCam), 眼睛位置(全局.Zen))) < 20;
    }
    动作 {
        等待(2.5, 无视条件);
        根据条件中止(比较(全局.CurrentCam, !=, 首个(全局.ZenPickedCamAndPos)));
        调用子程序(ZenJumpscare);
    }
}

规则 ("[SR] Zen at Doors") {
    事件 {
        子程序;
        ZenDoor;
    }
    动作 {
        设置全局变量(ZenDoorNumber, 随机整数(1, 3));
        If(比较(全局.ZenDoorNumber, ==, 1));
            等待直到 (非(与(在视线内(眼睛位置(全局.Player_), 矢量(1.93, 8.43, -30.57), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(1.93, 8.43, -30.57), 75))), 99999);
            传送(全局.Zen, 矢量(3.39, 7.06, -31.06));
            等待(减(6, 除(全局.ZenAI, 10)), 无视条件);
            等待直到 (非(全局.CurrentCam), 4);
            If(全局.ClosedLeftDoor);
                等待直到 (非(或(全局.CurrentCam, 与(在视线内(眼睛位置(全局.Player_), 眼睛位置(全局.Bastion), 屏障不会阻挡视线), 在视野内(全局.Player_, 眼睛位置(全局.Bastion), 75)))), 99999);
                If(全局.ClosedLeftDoor);
                    设置全局变量(ZenDoorNumber, 0);
                Else;
                    调用子程序(ZenJumpscare);
                End;
            Else;
                调用子程序(ZenJumpscare);
            End;
        Else If(比较(全局.ZenDoorNumber, ==, 2));
            等待直到 (非(或(全局.CurrentCam, 与(在视线内(眼睛位置(全局.Player_), 矢量(-4.38, 9, -23.22), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(-4.38, 9, -23.22), 75)))), 99999);
            传送(全局.Zen, 矢量(-4.38, 7.04, -23.22));
            等待(减(6, 除(全局.ZenAI, 10)), 无视条件);
            等待直到 (非(全局.CurrentCam), 4);
            If(全局.ClosedRightDoor);
                等待直到 (非(或(全局.CurrentCam, 与(在视线内(眼睛位置(全局.Player_), 眼睛位置(全局.Zen), 屏障不会阻挡视线), 在视野内(全局.Player_, 眼睛位置(全局.Zen), 75)))), 99999);
                If(全局.ClosedRightDoor);
                    设置全局变量(ZenDoorNumber, 0);
                Else;
                    调用子程序(ZenJumpscare);
                End;
            Else;
                调用子程序(ZenJumpscare);
            End;
        Else If(比较(全局.ZenDoorNumber, ==, 3));
            等待直到 (非(或(全局.CurrentCam, 与(在视线内(眼睛位置(全局.Player_), 矢量(6.74, 9.02, -19.94), 屏障不会阻挡视线), 在视野内(全局.Player_, 矢量(6.74, 9.02, -19.94), 75)))), 99999);
            传送(全局.Zen, 矢量(6.74, 7.02, -19.94));
            等待(减(6, 除(全局.ZenAI, 10)), 无视条件);
            等待直到 (非(全局.CurrentCam), 4);
            If(全局.ClosedFrontDoor);
                等待直到 (非(或(全局.CurrentCam, 与(在视线内(眼睛位置(全局.Player_), 眼睛位置(全局.Zen), 屏障不会阻挡视线), 在视野内(全局.Player_, 眼睛位置(全局.Zen), 75)))), 99999);
                If(全局.ClosedFrontDoor);
                    设置全局变量(ZenDoorNumber, 0);
                Else;
                    调用子程序(ZenJumpscare);
                End;
            Else;
                调用子程序(ZenJumpscare);
            End;
            调用子程序(ZenMove);
        End;
    }
}

规则 ("[SR] Zen Move") {
    事件 {
        子程序;
        ZenMove;
    }
    动作 {
        在索引处设置全局变量(ZenPickedCamAndPos, 0, 数组随机取值(从数组中移除(数组(1, 2, 3, 4, 5, 6, 7), 首个(全局.ZenPickedCamAndPos))));
        在索引处设置全局变量(ZenPickedCamAndPos, 1, 数组随机取值(数组中的值(全局.ZenPositions2, 首个(全局.ZenPickedCamAndPos))));
        等待直到 (比较(全局.CurrentCam, !=, 首个(全局.ZenPickedCamAndPos)), 99999);
        传送(全局.Zen, 数组中的值(全局.ZenPickedCamAndPos, 1));
    }
}

