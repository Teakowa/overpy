变量 {
    全局:
        0: A
        1: B
        2: C
        3: owo
    玩家:
        0: A
}
规则 ("1") {
    事件 {
        持续 - 每名玩家;
        双方;
        全部;
    }
    条件 {
        全局.A == 全局.B;
    }
    动作 {
        "A = 543+B"
        设置全局变量(A, 加(543, 全局.B));
        "A = A.x + A.y"
        设置全局变量(A, 加(X方向分量(全局.A), Y方向分量(全局.A)));
        "A = 2, B = 3"
        设置全局变量(A, 2);
        "A = 2, B = 3"
        设置全局变量(B, 3);
        "A = 2 + B + C"
        设置全局变量(A, 加(加(2, 全局.B), 全局.C));
        "A = B - 3"
        设置全局变量(A, 减(全局.B, 3));
        "A = B - C"
        设置全局变量(A, 减(全局.B, 全局.C));
        "A = B - C"
        设置全局变量(A, 减(全局.B, 全局.C));
        "A = 5, B = (5+B) - 6"
        设置全局变量(A, 5);
        "A = 5, B = (5+B) - 6"
        设置全局变量(B, 减(加(5, 全局.B), 6));
        "hostPlayer.A = 3"
        设置玩家变量(主机玩家, A, 3);
        If(全局.A);
            If(全局.A);
                设置全局变量(B, 3);
            End;
            While(全局.B);
                修改全局变量(C, 加, 1);
            End;
        End;
        While(全局.A);
            If(全局.A);
                设置全局变量(B, 3);
            End;
        End;
        If(全局.A);
            设置全局变量(B, 3);
        End;
        "hostPlayer.A = 4"
        设置玩家变量(主机玩家, A, 4);
    }
}

规则 ("2") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 123);
    }
}

规则 ("3") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(A, 456);
    }
}

规则 ("4") {
    事件 {
        持续 - 全局;
    }
    动作 {
        设置全局变量(B, 789);
    }
}

